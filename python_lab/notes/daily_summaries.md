## 2026年5月2日 训练总结

今天完成了 Phase 1 的前三道基础题目，成功激活了以下核心编程状态和知识点：

### 1. 哈希表 (Hash Map) 与空间换时间 (Problem 001: Two Sum)
- **知识点**：利用 Python 字典 (`dict`) 的 $O(1)$ 查找特性。
- **技巧**：**边历边存 (One-Pass Lookup)**。在遍历数组时，将看过的元素存入字典（值作为 Key，索引作为 Value）。与其向后用双层 `for` 循环寻找配对 ($O(n^2)$)，不如每次计算出需要的“那一半”，直接去字典里以 $O(1)$ 的速度验证，从而将总体时间复杂度优化到 $O(n)$。

### 2. 栈 (Stack) 结构与括号消除 (Problem 002: Valid Parentheses)
- **知识点**：利用 Python 列表 (`list`) 的 `append()` 和 `pop()` 方法来模拟 **后进先出 (LIFO/FILO)** 的栈结构。
- **技巧**：碰到具有“嵌套匹配”性质的问题时，将开头元素压入栈中，遇到结尾元素时弹出栈顶进行配对。
- **工程细节**：
  - 避免使用 Python 关键字（如 `str`, `list`, `dict`）作为变量名。
  - 对于固定的映射关系表（如括号匹配规则 `{"(": ")"}` 等），应当定义在循环外部进行初始化，避免内部重复创建带来性能损耗。

### 3. 双指针 (Two Pointers) 逆向填坑 (Problem 003: Merge Sorted Array)
- **知识点**：使用多个指针在数组上移动以完成查找或修改任务。
- **技巧**：**原地修改 (In-place) 与逆向思维**。当需要原地修改数组、且目标数组尾部带有足够的空余空间时，如果从前往后修改容易覆盖尚未读取的数据。此时采用“从后往前”填坑的策略可以完美避免元素平移，保持 $O(n)$ 时间复杂度和 $O(1)$ 空间复杂度。

## 2026年5月4日 训练总结

### 今天完成的题目

1. **Problem 004: Best Time to Buy and Sell Stock (买卖股票的最佳时机)**
   - **类型**: Array / Dynamic Programming
   - **收获记录 / 踩坑反思**:
     - 警惕嵌套循环和复杂的标志位，容易导致逻辑混乱和漏算最优解。
     - **注意隐性的时间复杂度退化**：在 `for` 循环中如果使用 Python 切片 `prices[0:i]` 以及 `min()` 等内置函数，虽然表面上是一层循环，但背后耗时是 $O(N^2)$，并且存在隐性的额外空间开销。
     - **正确的核心模式**（动态维护历史极值）：不需要每次都回头重新计算历史数据极值，而是在一次遍历（One Pass）中，引入状态变量（如 `min_buy_price`）进行常数级别的动态更新。这把算法优化到了真正的时间 $O(N)$ 和空间 $O(1)$。

2. **Problem 005: Longest Substring Without Repeating Characters (无重复字符的最长子串)**
   - **类型**: Hash Map / Sliding Window / String
   - **收获记录 / 踩坑反思**:
     - 表现堪称完美，一遍通过所有的逻辑和测试用例！
     - **正确的核心模式**（滑动窗口优化版）：不再用普通的 `while` 循环让左侧指针一点点紧缩，而是直接用哈希表记录字符“最后一次出现的索引”。当发现右指针遇到了处于当前窗口内的重复字符时，直接让左指针**跨步跳跃**到安全位置。这是一种极其优雅且高阶的 $O(N)$ 做法。

### 综合评价
今天的状态进步非常明显！从 004 题中的逻辑卡壳和复杂度踩坑，到 005 题直接写出零暇疵的滑动窗口跳跃式最优解，你的算法体感回来得很快。继续保持！明天可以继续挑战滑动窗口或双指针的其他高频变种。

---

## 2026年5月5日 训练总结

### 今天完成的题目

1. **Problem 006: Container With Most Water (盛最多水的容器)**
   - **类型**: Array / Two Pointers
   - **收获记录 / 踩坑反思**:
     - 表现非常出色，一次性写出最优解。
     - **正确的核心模式**（双指针收敛）：左右指针分别放置在两端。每次计算体积后，有策略地移动指向“较短”柱子的指针，以期望在更窄的宽度下遇到更高的柱子获取更大面积。（时间复杂度 $O(N)$，空间复杂度 $O(1)$）。

2. **Problem 007: Minimum Size Subarray Sum (长度最小的子数组)**
   - **类型**: Array / Sliding Window
   - **收获记录 / 踩坑反思**:
     - 逻辑顺畅，一次性完美实现了滑动窗口模式。
     - **正确的核心模式**（动态扩张与收缩）：右指针持续向右扩张寻找满足区间，当满足目标条件时（窗口和 $\ge$ target），立刻让左指针向右一步步逼近以不断缩小窗口寻找更优解。
     - **工程细节**：在需要求最小值的场景初始化变量时，推荐使用 `float('inf')` 替代硬编码的极大常数值（如 `10**5 + 1`），能够提升代码的健壮性和通用性。

### 综合评价
今天状态极佳！两道经典的指针边界题（双指针和滑动窗口）全部一次性写出最优解，没有任何逻辑漏洞。对指针移动的贪心策略和窗口收放的把控非常精准，对这些模式的肌肉记忆已经形成，非常棒！明天可以尝试引入限制条件的滑动窗口或者栈等相关问题。

---

## 2026年5月6日 训练总结

### 今天完成的题目

1. **Problem 008: Search Insert Position (搜索插入位置)**
   - **类型**: Array / Binary Search
   - **收获记录 / 踩坑反思**:
     - 一次性写出标准二分查找解法，可见测试全部通过。
     - 能正确处理目标值存在、插入中间、插入头部、插入尾部、单元素数组等边界情况。
     - **正确的核心模式**（二分查找 / 插入点）：使用 `left` 和 `right` 维护搜索区间。循环结束时，如果没有找到目标值，`left` 正好表示第一个大于等于 `target` 的位置，也就是插入位置。
     - **工程细节**：虽然 Python 整数不会溢出，但 `mid = left + (right - left) // 2` 是很好的通用写法，值得保留。

2. **Problem 009: Reverse Linked List (反转链表)**
   - **类型**: Linked List / Pointer
   - **收获记录 / 踩坑反思**:
     - 在理解链表辅助函数和节点结构后，成功写出迭代版链表反转。
     - 对 `previous`、`current`、`next_node` 三个变量的职责理解到位。
     - 可见测试全部通过，包括空链表、单节点链表、双节点链表和多节点链表。
     - **正确的核心模式**（链表反转三指针）：修改 `current.next` 之前，必须先保存 `next_node = current.next`，避免断链后丢失后续节点。之后再让 `current.next` 指向 `previous`，并推动两个指针前进。

### 今天新增掌握的模式

- **Binary Search - Lower Bound**：在有序数组中查找目标或插入位置，关键是理解循环结束后 `left` 的含义。
- **Linked List Reverse**：链表题不是交换值，而是改变节点之间的 `next` 指向；核心习惯是“断链前先保存后路”。

### 综合评价

今天的训练从数组题推进到了链表题，覆盖面明显扩大。二分查找完成得很稳，说明你对有序数组的边界收缩已经有了清晰手感。链表题虽然一开始需要解释辅助函数和节点模型，但理解后能正确写出三指针反转，这是非常关键的一步。下一次可以继续练链表的相邻节点操作，例如合并两个有序链表，或者回到栈/队列做一题模式巩固。

## 2026年5月8日 训练总结

### 今天完成的题目

1. **Problem 010: Binary Tree Inorder Traversal (二叉树的中序遍历)**
   - **类型**: Tree Traversal / DFS / Stack
   - **收获记录 / 踩坑反思**:
     - 了解了 `list.extend()` 与 `append()` 的底层行为差异。
     - 迭代/栈版一次性写对，掌握了利用 `while current or stack:` 这个经典的二叉树栈消除递归模板写法。

2. **Problem 011: Binary Tree Level Order Traversal (二叉树的层序遍历)**
   - **类型**: Tree Traversal / BFS / Queue
   - **收获记录 / 踩坑反思**:
     - 明白了输入的一维数据如何在内存中重构成层级结构。
     - 掌了 `collections.deque` 实现 $O(1)$ 的出队。
     - 熟练运用 `size = len(queue)` 和 `for _ in range(size)` 组合技，在不断增长的队列中像一把“剪刀”一样精准分层。

3. **Problem 012: Maximum Depth of Binary Tree (二叉树的最大深度)**
   - **类型**: Tree Traversal / DFS / Recursion
   - **收获记录 / 踩坑反思**:
     - 完美掌握了“把大问题拆解成子问题”的后序求信息模板：当前树最大深度 = `max(左孩子, 右孩子) + 1`。代码写得非常成熟和简洁。

### 综合评价
今天的状态极佳！连续攻克了树结构最核心的遍历三板斧（纯递归 DFS、栈迭代 DFS、队列层序 BFS），所有代码全部是一次性稳健跑通的。这是计算机底层极为核心的数据结构操作，你今天的肌肉记忆形成得非常好。好好休息，我们下次继续！

---

## 2026年5月9日 训练总结

### 今天完成的题目

1. **Problem 013: Same Tree (相同的树)**
   - **类型**: Tree Traversal / DFS / Recursion
   - **收获记录 / 踩坑反思**:
     - 一次性写出正确解法，可见测试全部通过。
     - 清晰掌握了“两棵树同步递归”的基本判断顺序：两个都为空、只有一个为空、当前值不同、递归比较左右子树。
     - 关键模式是同方向比较：`p.left` vs `q.left`，`p.right` vs `q.right`。

2. **Problem 014: Symmetric Tree (对称二叉树)**
   - **类型**: Tree Traversal / DFS / Recursion
   - **收获记录 / 踩坑反思**:
     - 在提示后写出了标准的镜像递归解法，可见测试全部通过。
     - 理解了它和 Same Tree 的区别：Same Tree 是同方向比较，对称树是交叉方向比较。
     - 关键模式是镜像比较：`left.left` vs `right.right`，`left.right` vs `right.left`。

3. **Problem 015: Path Sum (路径总和)**
   - **类型**: Tree Traversal / DFS / Recursion
   - **收获记录 / 踩坑反思**:
     - 一次性写出正确解法，可见测试全部通过。
     - 正确理解了“路径必须从根节点到叶子节点”，没有把中间节点误判为合法路径。
     - 当前实现逻辑正确，但控制流略绕：先递归左右，再判断叶子。后续可以练习把“叶子节点判断”提前，让递归出口更直接。

### 今天新增掌握的模式

- **双树同步递归**：用于判断两棵树是否相同、子树是否匹配等问题。
- **镜像递归**：用于判断对称、翻转后是否相同等问题，核心是交叉比较。
- **根到叶路径递归**：递归时携带“剩余目标值”或“当前累计和”，只在叶子节点判断答案。

### 综合评价

今天完整推进了树递归的三种高频形态：同方向双树比较、镜像交叉比较、根到叶路径判断。你在 Same Tree 和 Path Sum 上都能一次性写对，说明递归边界条件和子问题拆分已经明显变稳。Symmetric Tree 在提示后也能快速转换成正确代码，说明你已经开始能分辨树题里“比较方向”的差异了。

下一次建议从 **Problem 016: Minimum Depth of Binary Tree** 开始。它看起来像 Maximum Depth 的简单变体，但真正训练点是：最小深度必须到叶子节点，不能把空子树深度 `0` 直接拿去参与 `min()`。

---

## 2026年5月10日 训练总结

### 今天完成的题目

1. **Problem 016: Invert Binary Tree (翻转二叉树)**
   - **类型**: Tree Traversal / DFS / Recursion
   - **收获记录 / 踩坑反思**:
     - 一次性写出正确解法，可见测试全部通过。
     - 掌握了二叉树原地修改递归：对每个节点交换左右孩子，并递归处理整棵树。
     - 当前写法逻辑正确，但交换变量时可以去掉多余的临时初始化，后续可以练习 Python 的 tuple unpacking 写法。

2. **Problem 017: Binary Tree Paths (二叉树的所有路径)**
   - **类型**: Tree Traversal / DFS / Recursion
   - **收获记录 / 踩坑反思**:
     - 一次性写出正确解法，可见测试全部通过。
     - 重点理解了递归函数的返回值：`binary_tree_paths(node)` 返回的是“从当前节点到所有叶子节点的路径字符串列表”。
     - 进一步澄清了 `left_paths` 和 `right_paths` 的含义：它们不是节点、不是个数，而是 `List[str]`，列表中的每个元素是一条完整的子树路径字符串。
     - 明白了叶子节点负责创建路径，中间节点负责给子路径加前缀，根节点最终返回整棵树的完整路径列表。

3. **Problem 018: Path Sum II (路径总和 II)**
   - **类型**: Tree Traversal / DFS / Recursion / Backtracking
   - **收获记录 / 踩坑反思**:
     - 一次性写出正确解法，可见测试全部通过。
     - 成功把 Problem 015 的“剩余目标值递归”和 Problem 017 的“递归返回路径列表”结合起来。
     - 当前解法不是标准 append/pop 回溯，而是子树返回合法路径列表、父节点加前缀；这是正确的 DFS 模型。
     - 理解了分别遍历左右子树路径可以替代 `left_paths + right_paths`，避免额外创建一个临时合并列表。

### 今天新增掌握的模式

- **二叉树原地修改递归**：函数不仅返回结果，还会直接修改节点指针，例如翻转二叉树。
- **递归返回路径列表**：子树返回局部答案，父节点加工成更完整的答案。
- **剩余目标值递归**：每深入一层就扣掉当前节点值，到叶子节点时判断是否刚好命中目标。
- **树上路径收集**：叶子节点创建路径，中间节点拼接或加工路径，根节点汇总最终答案。

### 综合评价

今天的重点不是题目数量，而是把“递归返回值到底是什么”这件事掰开揉碎地想清楚。你从一开始不确定 `left_paths` / `right_paths` 是字符、节点值还是个数，到后来理解它们是“子树已经完成的路径列表”，这是非常关键的一步。

接下来建议进入标准回溯训练：用一个共享的 `path` 列表，进入节点时 `append`，离开节点时 `pop`。这会为后面的子集、组合、排列、DFS 搜索题打基础。

## 2026年5月11日 训练总结

### 今天完成的题目

1. **Problem 019: Subsets (子集)**
   - **类型**: Backtracking / DFS / Array
   - **收获记录 / 踩坑反思**:
     - 写出了标准回溯解法，可见测试全部通过。
     - 理解了 `backtrack(start)` 的含义：在当前 `path` 已经选好的基础上，只从 `nums[start:]` 继续选择。
     - 掌握了组合 / 子集型回溯的关键：使用 `start` 控制下一层搜索范围，避免重复选择和逆序组合。
     - 明确了 `path.copy()` 的必要性：保存当前路径快照，避免后续 `path.pop()` 修改已经加入结果的列表。

2. **Problem 020: Permutations (全排列)**
   - **类型**: Backtracking / DFS / Array
   - **收获记录 / 踩坑反思**:
     - 写出了标准全排列回溯解法，可见测试全部通过。
     - 掌握了排列型回溯的关键：每一层都可以从所有元素里选择，但同一个元素在当前路径中只能使用一次。
     - 正确使用 `used` 数组记录当前路径里已经使用过的元素。
     - 能保持状态恢复成对出现：`path.append()` 对应 `path.pop()`，`used[i] = True` 对应 `used[i] = False`。

### 今天新增掌握的模式

- **回溯三步**：做选择、递归、撤销选择。
- **组合 / 子集型搜索使用 `start`**：下一层只从当前位置之后继续选，避免重复组合。
- **排列型搜索使用 `used`**：每一层从全部元素中选，但跳过当前路径里已经使用过的元素。
- **路径快照**：把 `path` 加入结果时要使用 `path.copy()`，不能直接保存共享列表对象。

### 综合评价

今天正式从树递归过渡到了标准回溯。第 19 题解决的是“选哪些元素”，第 20 题解决的是“按什么顺序选所有元素”。这两个题放在一起非常关键，因为它们建立了回溯里最常见的分叉：

- 子集 / 组合：用 `start`。
- 排列：用 `used`。

今天不只是把代码写通了，还追问了回溯代码的语法和执行过程，这比单纯刷过一题更有价值。下一次建议继续做组合题，例如 **Combinations** 或 **Combination Sum**，用来巩固 `start`、剪枝和递归终止条件。

## 2026年5月12日 训练总结

### 今天完成的题目

1. **Problem 021: Combination Sum (组合总和)**
   - **类型**: Backtracking / DFS / Array
   - **收获记录 / 踩坑反思**:
     - 完美实现出隐式回溯并结合了精妙的提前剪枝（对 candidates 排序，若 `candidates[i] > target` 时直接 break）。
     - 掌握了元素可重复选取的回溯模型：下一层递归的起始位置依然从 `i` 开始。

2. **Problem 022: Combination Sum II (组合总和 II)**
   - **类型**: Backtracking / DFS / Array
   - **收获记录 / 踩坑反思**:
     - 完美一次性写出教科书级别的“同层去重”逻辑：`if i > start and candidates[i] == candidates[i - 1]: continue`。
     - 理解了“每个数字只能使用一次的情况下通过 `i + 1` 向下层传递”。

3. **Problem 023: Permutations II (全排列 II)**
   - **类型**: Backtracking / DFS / Array
   - **收获记录 / 踩坑反思**:
     - 完美实现了进阶挑战，深刻理解了“显式回溯”（`append` + `pop` 配合 `copy()`）与“隐式回溯”（传 `path + [val]`）的底层区别。
     - 准确梳理出了全排列中独特的同层去重逻辑：`if i > 0 and nums[i] == nums[i - 1] and not used[i - 1]: continue`，非常准确地利用 `used` 数组区分了树枝重复与树层重复。

### 今天新增掌握的模式

- **隐式回溯 vs 显式回溯**：隐式利用列表 `+` 产生新对象，代码极简易读；显式利用同一列表对象不断 `append`/`pop`，内存表现与性能极佳，但记录结果时须配合 `path.copy()`。
- **组合去重模式**：排序后，通过 `i > start` 避免同一层级选到相同元素。
- **排列去重模式**：排序后，通过 `not used[i - 1]` 巧妙识别出同一树层中是否发生交叉重复。

### 综合评价
今天状态极佳！连续三道带“去重、无限重复选取”的高频回溯变形题全部不仅逻辑通顺，而且全部一遍过。除此之外，不仅在算法逻辑框架上形成了极稳的肌肉记忆，还深入探讨了 Python 语言特性层面的内存管理差异。这些高质量的训练为之后的子集类问题、甚至是高难度暴搜剪枝打下了最为坚实的基础。继续保持！

---

## 2026年5月13日 训练总结

### 今天完成的题目

1. **Problem 024: Subsets II (子集 II)**
   - **类型**: Backtracking / DFS / Array
   - **收获记录 / 踩坑反思**:
     - 正确沿用了 Problem 019 的子集回溯结构，并理解了本题和 019 的核心区别：019 输入不重复，024 输入可能重复。
     - 掌握了子集 / 组合型搜索的同层去重条件：`if i > start and nums[i] == nums[i - 1]: continue`。
     - 明确了 `i > start` 的含义：只跳过同一层的重复选择，不影响下一层继续选择重复值。

2. **Problem 025: Generate Parentheses (括号生成)**
   - **类型**: Backtracking / DFS / String
   - **收获记录 / 踩坑反思**:
     - 正确使用 `left_count` 和 `right_count` 两个状态变量控制搜索。
     - 掌握了合法括号生成的两个约束：左括号数量小于 `n` 时可以加左括号；右括号数量小于左括号数量时才可以加右括号。
     - 理解了隐式回溯和显式回溯在本题中的写法区别：字符串拼接会创建新状态，共享 `path` 列表则必须 `append` 后 `pop`。

3. **Problem 026: Letter Combinations of a Phone Number (电话号码的字母组合)**
   - **类型**: Backtracking / DFS / String / Hash Map
   - **收获记录 / 踩坑反思**:
     - 正确理解了 `index` 的含义：它不是当前选哪个字母，而是当前正在处理 `digits` 的第几个数字。
     - 掌握了“位置驱动的回溯”：每一层递归对应输入字符串的一个位置，每层候选集合由当前数字查字典得到。
     - 理解了 `"".join(path)` 的作用：把 `["a", "d"]` 这样的字符列表拼成 `"ad"`，用于保存最终答案。
     - 进一步理解了递归和 `for` 循环的配合：递归负责深入下一层，当前层的 `for` 循环负责横向尝试下一个候选。

### 今天新增掌握的模式

- **子集去重模式**：排序后用 `i > start` 判断同层重复。
- **约束型回溯**：不是先生成所有情况再过滤，而是在生成过程中用条件剪掉非法分支。
- **位置驱动回溯**：递归参数 `index` 表示当前处理输入的哪个位置。
- **显式回溯状态恢复**：共享 `path` 修改后必须恢复，`append` 和 `pop` 成对出现。
- **结果拼接**：当结果需要字符串而 `path` 是字符列表时，使用 `"".join(path)`。

### 综合评价

今天的重点不是题目难度，而是把回溯的执行过程真正看清楚了。你不只是写过了 024、025、026，还追问了几个非常关键的问题：为什么 `path` 可以放在外层或参数里，`join` 到底做了什么，`index = 0` 时递归树怎么展开，以及为什么从 `"a"` 分支返回后不会重新选 `"a"`，而是继续选 `"b"`。

这些问题说明你已经开始从“套模板”进入“理解程序执行流”的阶段。下一次建议继续做回溯字符串题，例如 **Palindrome Partitioning**，它会继续训练 `index` 模型，但会额外加入“切分字符串”和“判断回文”的逻辑。

---

## 2026年5月14日 训练总结

### 今天完成的题目

1. **Problem 027: Palindrome Partitioning (分割回文串)**
   - **类型**: Backtracking / DFS / String
   - **收获记录 / 踩坑反思**:
     - 第一版使用“递归处理剩余字符串”的方式完成了题目，测试通过，说明你已经理解了“每次切一个回文前缀，再处理剩余部分”的递归思想。
     - 之后根据 review 改成了更标准的 `start + path` 显式回溯版本，训练价值更高。
     - 掌握了切割型回溯的核心：`start` 表示当前从哪里开始切，循环枚举右边界，合法子串才进入下一层。
     - 理解了 `path.copy()` 的作用：保存当前切割方案的快照，避免后续 `pop()` 修改已经加入结果的路径。
     - 复习了回文判断：`text == text[::-1]` 表示判断字符串是否等于它的反转结果。

### 今天新增掌握的模式

- **切割型回溯**：不是“选某个元素”，而是决定当前这一刀切到哪里。
- **`start` 下标语义**：`start` 表示下一段子串的起始位置；当 `start == len(s)` 时，说明整个字符串已经切完。
- **合法性剪枝**：只有当前子串是回文串，才值得继续递归。
- **显式回溯模板**：`append`、递归、`pop` 必须成对出现。
- **Python 切片反转**：`text[::-1]` 会生成反转后的字符串，可用于简单判断回文。

### 综合评价

今天这题不是只“做对了”，而是经历了一个很有价值的改进过程：先用自然递归写通，再把结构整理成标准回溯模型。这个过程很重要，因为真实刷题和工程训练里，第一版能跑不代表结构最好；你需要学会在正确的基础上继续整理状态定义。

下一次建议继续做字符串回溯题，可以进入 **Problem 028: Restore IP Addresses**。它和今天这题同属切割型回溯，但会额外训练“每段长度限制”和“数值合法性判断”。

---

## 2026年5月15日 训练总结

### 今天完成的题目

1. **Problem 028: Word Search (单词搜索)**
   - **类型**: Backtracking / DFS / Matrix
   - **收获记录 / 踩坑反思**:
     - 一开始完全没有思路，主要卡在从一维回溯切换到二维网格搜索。
     - 通过把递归状态定义为 `dfs(i, j, k)`，成功建立了网格 DFS 的核心模型：当前位置是 `(i, j)`，当前要匹配的是 `word[k]`。
     - 正确处理了三个基础失败条件：越界、字符不匹配、当前路径重复访问。
     - 使用原地标记 `board[i][j] = "#"` 表示当前路径已经访问过该格子，并在递归返回后恢复原字符。
     - 能够从每个格子尝试作为起点，并在四个方向中任意一个方向成功时立刻返回 `True`。

### 今天新增掌握的模式

- **二维网格 DFS**：递归状态通常包含行、列、目标下标，例如 `dfs(i, j, k)`。
- **路径级访问控制**：同一个格子不能在同一条路径中重复使用，但不同搜索路径之间可以重新使用。
- **原地访问标记**：临时修改 `board[i][j]` 可以省掉额外 `visited` 数组，但必须在递归结束后恢复。
- **短路搜索**：只要上下左右四个方向中有一个方向成功，就可以立即返回成功。
- **从所有起点尝试**：网格搜索通常需要外层双循环枚举起点。

### 综合评价

今天最大的价值不是题目数量，而是完成了一次重要迁移：从一维字符串 / 数组回溯，进入二维网格回溯。你最开始觉得这题“太难”，但最后写出的代码是标准解法，说明问题不在能力，而在缺少网格 DFS 的状态模型。

下一次建议继续巩固二维 DFS，可以做 **Number of Islands** 或 **Surrounded Regions**。如果想继续回溯路线，也可以做 **Restore IP Addresses**，它会回到字符串切割型回溯，但合法性判断更复杂。

---

## 2026年5月16日 训练总结

### 今天完成的题目

1. **Problem 029: Number of Islands (岛屿数量)**
   - **类型**: DFS / Matrix / Connected Components
   - **收获记录 / 踩坑反思**:
     - 正确写出外层双循环扫描整张网格的结构。
     - 遇到尚未访问的陆地 `"1"` 时，能先将岛屿数量加 1，再用 DFS 把同一座岛屿上的所有陆地标记为 `"0"`。
     - 理解了这题不是找某一条路径，而是“清空一个连通块”：DFS 的目标是把当前岛屿整体访问完，避免之后重复计数。
     - 明确了 `count` 统计的不是岛屿里的所有格子，而是每座岛在扫描顺序中第一次被遇到的“入口点 / 代表点”。

### 今天新增掌握的模式

- **连通块计数模板**：外层遍历负责发现新的连通块入口，DFS/BFS 负责访问完整个连通块。
- **网格 DFS 状态简化**：和 Word Search 不同，Number of Islands 的递归状态只需要坐标 `(r, c)`，不需要字符串下标 `k`。
- **原地访问标记**：把 `"1"` 改成 `"0"` 可以表示该陆地已经访问过，节省额外 `visited` 空间。
- **代表点理解**：一整片岛屿会被 DFS 消掉，所以外层循环只会为每个连通块计数一次。
- **DFS vs BFS 区别**：两者都能访问相邻点；DFS 是“一条路走到底再回头”，BFS 是“一圈一圈向外扩散”。当题目只要求清空连通块时两者都可用；当题目要求最短距离、最少步数、扩散时间时，优先考虑 BFS。

### 综合评价

今天的重点是从“会写 DFS”进一步推进到“理解 DFS/BFS 背后的遍历范式”。你已经能看出 Number of Islands 本质上是在数“连通块代表点”，这是一个很重要的抽象能力。

这类题之后会反复出现：岛屿最大面积、Flood Fill、Surrounded Regions、Rotting Oranges、图的连通分量数量，本质上都在复用“节点 + 邻居 + visited + 遍历”的模型。下一次建议做 **Max Area of Island** 巩固 DFS 返回值，或者做 **Rotting Oranges** 正式训练多源 BFS 和层数。

---

## 2026年5月17日 训练总结

### 今天完成的题目

1. **Problem 030: Rotting Oranges (腐烂的橘子)**
   - **类型**: BFS / Queue / Matrix / Multi-source BFS
   - **收获记录 / 踩坑反思**:
     - 从 Number of Islands 的“连通块”视角自然迁移到了 BFS 扩散问题。
     - 理解了这题不是让每个腐烂橘子单独遍历全岛，而是所有腐烂橘子同时扩散。
     - 正确使用 `deque` 保存所有初始腐烂橘子，实现多源 BFS。
     - 掌握了 `for _ in range(len(queue))` 的层序遍历写法：每一轮只处理当前队列已有的节点，因此每一层代表 1 分钟。
     - 通过把新鲜橘子从 `1` 改成 `2`，完成访问标记，避免重复入队。

2. **Problem 031: Climbing Stairs (爬楼梯)**
   - **类型**: Dynamic Programming / Math
   - **收获记录 / 踩坑反思**:
     - 正式进入 DP 训练。
     - 学会了 DP 的五步法：定义状态、初始值、转移方程、遍历顺序、返回答案。
     - 正确写出 `dp[i] = dp[i - 1] + dp[i - 2]`，并理解它来自“最后一步从第 `i - 1` 阶或第 `i - 2` 阶过来”。
     - 使用数组版 DP 完成题目，测试全部通过。

3. **Problem 032: Min Cost Climbing Stairs (使用最小花费爬楼梯)**
   - **类型**: Dynamic Programming / Array
   - **收获记录 / 踩坑反思**:
     - 测试全部通过，能正确处理“楼顶不是 cost 数组中的某个下标”这个关键边界。
     - 发现了 DP 中很重要的问题：代码能过，不代表状态定义完全干净。
     - 当前实现更接近“踩到第 i 阶并支付 cost[i] 后的最小成本”，而注释写成了“到达第 i 阶的最小成本”，两者初始值不同。
     - 明确了状态定义、初始值、转移方程三者必须严格自洽。

### 今天新增掌握的模式

- **多源 BFS**：多个起点同时进入队列，一起按层扩散。
- **BFS 层序计时**：分钟数、最短距离这类问题，通常每处理完一层再加 1。
- **`_` 占位变量**：当循环变量不会被使用时，可以用 `_` 表示“只需要循环次数”。
- **二维网格行列含义**：`len(grid)` 是行数，`len(grid[0])` 是列数。
- **DP 五步法**：状态定义 -> 初始值 -> 转移方程 -> 遍历顺序 -> 返回答案。
- **DP 状态自洽性**：同一道题可以有不同状态定义，但每种定义必须配套自己的初始值和转移方程。

### 综合评价

今天完成了一个很关键的转折：上午还在网格 DFS/BFS，后面正式进入 DP。Rotting Oranges 说明你已经能把“连通块”进一步理解成“按层扩散的图搜索”；Climbing Stairs 和 Min Cost Climbing Stairs 则建立了 DP 的基本工作流。

今天最重要的收获不是多做了几题，而是开始注意“状态定义是否精确”。这是 DP 能不能稳定进步的分水岭。下一次建议继续做一维 DP，例如 **House Robber**，它会继续训练“当前选 / 不选”的状态转移。

---

## 2026年5月18日 训练总结

### 今天完成的题目

1. **Problem 033: House Robber (打家劫舍)**
   - **类型**: Dynamic Programming / Array
   - **收获记录 / 踩坑反思**:
     - 一次性写出标准 DP 数组解法，测试全部通过。
     - 掌握了“选择型 DP”的核心：面对第 `i` 间房屋时，要么偷当前房屋，要么不偷当前房屋。
     - 正确写出状态转移：`dp[i] = max(dp[i - 1], dp[i - 2] + nums[i])`。
     - 理解了 `dp = [0] * n` 的含义：提前创建长度为 `n` 的数组，用来保存每个位置的阶段性答案。
     - 明确了只依赖前两个状态的 DP 后续可以优化到 O(1) 空间。

2. **Problem 034: Maximum Subarray (最大子数组和)**
   - **类型**: Dynamic Programming / Array
   - **收获记录 / 踩坑反思**:
     - 一次性写出标准 DP 解法，测试全部通过。
     - 重点理解了 `dp[i]` 的局部含义：它表示“必须以第 `i` 个元素结尾的最大连续子数组和”。
     - 掌握了“接上前一段”与“从当前元素重新开始”的选择：`dp[i] = max(nums[i], dp[i - 1] + nums[i])`。
     - 理解了为什么最终答案不能直接返回 `dp[-1]`：最大子数组可能在中间结束。
     - 正确使用 `max_sum` 维护所有局部状态中的全局最大值。
     - 注意到全负数场景下答案不能初始化为 0，必须从 `nums[0]` 开始。

3. **Problem 035: Unique Paths (不同路径)**
   - **类型**: Dynamic Programming / Matrix
   - **收获记录 / 踩坑反思**:
     - 完成了第一道二维 DP 题，测试全部通过。
     - 正确使用 `dp[i][j]` 表示从左上角走到格子 `(i, j)` 的路径数量。
     - 掌握了二维表格中的来源关系：当前位置只能从上方或左方转移过来。
     - 正确写出状态转移：`dp[i][j] = dp[i - 1][j] + dp[i][j - 1]`。
     - 正确初始化第一行和第一列为 1，因为这些位置都只有唯一走法。
     - 使用 `[[0] * n for _ in range(m)]` 创建二维数组，避免了 Python 中 `[[0] * n] * m` 的共享引用问题。

### 今天新增掌握的模式

- **选择型 DP**：当前元素选或不选，典型代表是 House Robber。
- **局部最优 DP**：`dp[i]` 不一定是全局答案，可能只是“带限制条件的局部最优”。
- **全局答案单独维护**：当最大值可能出现在中间位置时，需要额外变量记录全局最优。
- **重新开始 vs 接上前缀**：如果前面的连续和是负贡献，就从当前元素重新开始。
- **二维 DP 表格填充**：定义 `dp[i][j]`，先初始化边界，再按行列顺序填表。
- **边界初始化意识**：第一行、第一列、单元素、全负数这些边界会直接决定 DP 是否稳。
- **Python DP 数组创建**：一维数组可以用 `[0] * n`；二维数组优先用列表推导式创建独立行。

### 综合评价

今天是 DP 训练推进非常扎实的一天。你从一维的“选或不选”，走到“必须以当前位置结尾”的局部状态，再进入二维矩阵路径数。这个顺序很好，因为它把 DP 的几个核心问题都碰了一遍：状态定义、状态转移、初始化、返回值，以及局部状态和全局答案的区别。

尤其值得记录的是，你不只是把题做过了，还主动追问了 `dp = [0] * n`、Maximum Subarray 的转移逻辑、二维 DP 是什么。这些问题都很关键，说明你在补的不只是算法模板，而是在恢复 Python 代码执行和 DP 表格含义之间的连接。

下一次建议继续做二维 DP 的变体，例如 **Unique Paths II**。它会在今天的网格路径基础上加入障碍物，训练“边界初始化 + 条件阻断”的组合思维。

---

## 2026年5月19日 训练总结

### 今天完成的题目

1. **Problem 036: Unique Paths II (不同路径 II)**
   - **类型**: Dynamic Programming / Matrix
   - **收获记录 / 踩坑反思**:
     - 在上一题 `Unique Paths` 的基础上加入障碍物条件。
     - 正确理解了输入格式变化：上一题输入 `m, n`，本题输入完整二维网格 `obstacle_grid`。
     - 掌握了障碍格子的处理：如果 `obstacle_grid[i][j] == 1`，则 `dp[i][j] = 0`。
     - 明确第一行和第一列不能无脑初始化为 1；遇到障碍后，后续直线路径全部被截断。

2. **Problem 037: Minimum Path Sum (最小路径和)**
   - **类型**: Dynamic Programming / Matrix
   - **收获记录 / 踩坑反思**:
     - 从“路径数量 DP”切换到了“路径代价最优 DP”。
     - 正确写出 `dp[i][j] = min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j]`。
     - 明确第一行只能从左侧累加，第一列只能从上方累加。
     - 发现了练习文件中一个测试用例 expected value 写错的问题，并能用 DP 表和手算路径反查测试本身。这是很重要的工程判断：测试也可能错，不能盲信。

3. **Problem 038: Triangle (三角形最小路径和)**
   - **类型**: Dynamic Programming / Array
   - **收获记录 / 踩坑反思**:
     - 理解了三角形路径规则：每一步只能向下或向下向右，不能在下一行随便选数。
     - 正确处理不规则二维结构：第 `i` 行长度是 `i + 1`。
     - 正确区分边界来源：
       - `j == 0` 只能从上一行同列来；
       - `j == i` 只能从上一行 `j - 1` 来；
       - 中间位置从上一行 `j - 1` 和 `j` 中选择较小路径。
     - 明确最终答案不是固定右下角，而是最后一行的最小值。

### 今天新增掌握的模式

- **二维 DP 表格填充**：先定义 `dp[i][j]` 的含义，再处理初始化和状态转移。
- **路径数量 vs 路径代价**：
  - 路径数量题通常把来源相加；
  - 最小路径和题通常从多个来源里取 `min`。
- **边界初始化受题意影响**：
  - 无障碍路径题第一行/第一列都是 1；
  - 有障碍路径题遇到障碍后要停止；
  - 最小路径和题第一行/第一列要累加 cost。
- **答案位置需要根据题意判断**：
  - 矩形网格通常返回右下角；
  - Triangle 返回最后一行最小值。

### 综合评价

今天集中训练了 DP 的二维路径类问题，完成了从“数路径数量”到“求最小代价”，再到“不规则三角形结构”的过渡。整体状态很稳：第 36、38 题都能正确处理边界，第 37 题还能主动质疑错误测试用例，说明你已经开始用工程方式审查题目和测试，而不是只被动接受结果。

下次建议继续做 DP，但可以开始引入一维空间优化：先把今天的二维 DP 写法吃透，再练习如何只保留上一行或当前行状态。

---

## 2026年5月20日 训练总结

### 今天完成的题目

1. **Problem 039: Coin Change**
   - **类型**: Dynamic Programming / Complete Knapsack
   - **收获记录 / 踩坑反思**:
     - 掌握了“不可达状态”的设置方式：用 `amount + 1` 作为哨兵值。
     - 理解了为什么不能把所有状态初始化为 0：`0` 有真实含义，只能表示“凑出金额 0 需要 0 枚硬币”。
     - 明确了状态转移 `dp[x] = min(dp[x], dp[x - coin] + 1)` 的含义：如果能凑出 `x - coin`，再加一枚 `coin` 就能凑出 `x`。
     - 理解了小金额状态先被计算，大金额可以依赖更小金额逐步推导。

2. **Problem 040: Coin Change II**
   - **类型**: Dynamic Programming / Complete Knapsack
   - **收获记录 / 踩坑反思**:
     - 把目标从“最少硬币数”切换到了“组合数量”。
     - 明确了 `dp[x]` 表示凑出金额 `x` 的组合数量。
     - 理解了 `dp[0] = 1`：凑出 0 元有一种方式，即不选择任何硬币。
     - 掌握了 `dp[x] += dp[x - coin]`：这是把使用当前硬币新增的方案数累加到已有方案数中，不能写成覆盖赋值。
     - 理解了组合题中“先遍历硬币，再遍历金额”可以避免把不同顺序的同一组合重复计算。

3. **Problem 041: Partition Equal Subset Sum**
   - **类型**: Dynamic Programming / 0-1 Knapsack
   - **收获记录 / 踩坑反思**:
     - 学会把“能否分成两个等和子集”转换成“能否凑出总和的一半”。
     - 明确了 `dp[x]` 表示是否可以用已经遍历过的数字凑出和 `x`。
     - 理解了 0/1 背包的含义：每个物品只有选或不选两种状态，每个数字只能使用一次。
     - 掌握了倒序遍历容量的原因：避免同一个数字在同一轮更新中被重复使用。

### 今天新增掌握的模式

- **不可达状态**：用一个不可能成为合法答案的值表示当前状态还无法到达。
- **最少数量型 DP**：状态值记录最优数量，转移时通常使用 `min`。
- **方案数量型 DP**：状态值记录方案数，转移时通常使用加法累积。
- **完全背包**：物品可以重复使用，容量通常正序遍历。
- **0/1 背包**：物品只能使用一次，容量通常倒序遍历。
- **组合 vs 排列**：组合不关心顺序，排列关心顺序；循环顺序会直接影响是否重复计数。

### 综合评价

今天完成了 DP 训练里很关键的一组题：从路径型 DP 进入背包型 DP。你不仅写出了代码，还追问了“不可达状态为什么有效”“怎么判断金额被凑出来”“为什么方案数要累加”“为什么叫 0/1 背包以及为什么倒序遍历”。这些问题都问在了核心上。

目前需要继续巩固的是：看到题目时先判断状态值类型，是最优值、方案数，还是可达性。再判断物品能否重复使用，决定容量遍历方向。下一次建议继续做 **Target Sum**，它会把加减号问题转换成 0/1 背包的子集和问题。

---

## 2026年5月21日 训练总结

### 今天完成的题目

1. **Problem 042: Target Sum (目标和)**
   - **类型**: Dynamic Programming / 0-1 Knapsack / Count Ways
   - **收获记录 / 踩坑反思**:
     - 明确了“遍历所有 `+/-` 符号组合”属于暴力枚举，时间复杂度是 `O(2^n)`。
     - 理解了 Target Sum 的核心转换：
       - 加号组和为 `positive`
       - 减号组和为 `negative`
       - `positive - negative = target`
       - `positive + negative = sum(nums)`
       - 所以 `positive = (sum(nums) + target) / 2`
     - 成功把符号选择问题转换为：从 `nums` 中选若干个数，使其和等于 `positive`，一共有多少种选法。
     - 正确写出 0/1 背包方案数 DP：
       - `dp[x]` 表示凑出和 `x` 的方案数。
       - `dp[0] = 1` 表示空选择。
       - 每个数字只能用一次，所以容量倒序遍历。
       - 转移为 `dp[x] += dp[x - num]`。
     - 正确处理了 `0` 的特殊情况：`+0` 和 `-0` 表达式值相同，但符号方案不同，所以方案数会翻倍。
     - 单题测试和全量测试全部通过。

### 今天新增掌握的模式

- **符号选择转子集和**：当题目要求给每个数添加 `+/-`，可以尝试把数字分成加号组和减号组，再通过方程转换为子集和问题。
- **0/1 背包方案数**：每个元素只能选一次，容量倒序遍历，状态值表示方案数，转移使用累加。
- **非法容量判断**：转换前需要判断 `sum(nums) + target` 是否为负数或奇数；不合法时直接返回 `0`。

### 综合评价

今天的重点不是多刷题，而是把 Target Sum 这类背包变形真正想明白。你先确认了暴力枚举的本质，再追问公式含义，最后能自己写出正确 DP，并通过包含 `0` 的测试。这说明你不只是记模板，而是在理解模板从哪里来。

下一次建议继续做一题 0/1 背包的最大值或最小值变形，把“可达性 / 方案数 / 最优值”三种状态值彻底分开。

---

## 2026年5月22日 训练总结

### 今天完成的题目

1. **Problem 043: Longest Common Subsequence (最长公共子序列)**
   - **类型**: Dynamic Programming / String / 2D DP
   - **收获记录 / 踩坑反思**:
     - 正确写出二维 DP 解法，可见测试和补充边界测试全部通过。
     - 明确了 `dp[i][j]` 的含义：`text1` 的前 `i` 个字符和 `text2` 的前 `j` 个字符的最长公共子序列长度。
     - 理解了前缀 DP 中“比较末尾”的原因：当状态表示前 `i` / 前 `j` 个元素时，新增纳入考虑的元素就是当前前缀的末尾。
     - 重点理解了不相等时的转移：
       - `dp[i - 1][j]` 表示跳过 `text1` 当前末尾后的最优解。
       - `dp[i][j - 1]` 表示跳过 `text2` 当前末尾后的最优解。
       - 两者取较大值，就是当前状态能继承的最好结果。

2. **Problem 044: Uncrossed Lines (不相交的线)**
   - **类型**: Dynamic Programming / Array / 2D DP / Longest Common Subsequence
   - **收获记录 / 踩坑反思**:
     - 正确识别出这题本质是数组版本的 LCS。
     - 理解了“不相交线”的核心条件：匹配对在两个数组中的下标序列都必须单调递增。
     - 澄清了一个重要误区：不能比较单条线内部的 `i` 和 `j` 大小，因为它们属于不同数组；判断是否相交，要比较两条线之间的相对顺序。
     - 正确写出二维 DP 转移，可见测试和补充边界测试全部通过。

### 今天新增掌握的模式

- **二维前缀 DP**：用 `dp[i][j]` 表示两个输入前缀之间的最优结果，通常多开一行一列处理空前缀。
- **LCS 核心转移**：
  - 当前元素相等：`dp[i][j] = dp[i - 1][j - 1] + 1`
  - 当前元素不相等：`dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])`
- **不相交线转 LCS**：线不相交等价于匹配顺序一致，也就是选出的公共元素必须同时保持两个数组中的相对顺序。

### 综合评价

今天的重点不是题量，而是把二维 DP 的状态含义真正想清楚。你一开始对 `max(dp[i - 1][j], dp[i][j - 1])` 的含义有疑问，这是 LCS 最关键也最容易抽象的地方；通过可视化表格和“不相交线”的类比，你已经把“跳过一边当前元素”和“保持相对顺序”这两个核心概念连起来了。

下一次建议继续做一题 LCS 变形或编辑距离前置题，把二维 DP 的“状态定义 -> 选择 -> 转移”流程再巩固一遍。

---

## 2026年5月23日 训练总结

### 今天完成的题目

1. **Problem 045: Longest Palindromic Subsequence (最长回文子序列)**
   - **类型**: Dynamic Programming / String / Interval DP
   - **收获记录 / 踩坑反思**:
     - 正确区分了“子序列”和“子串”：子序列可以跳过字符，子串必须连续。
     - 正确使用 `dp[i][j]` 表示区间 `s[i:j+1]` 内的最长回文子序列长度。
     - 掌握了区间 DP 的核心遍历顺序：因为 `dp[i][j]` 依赖 `dp[i + 1][j]`、`dp[i][j - 1]`、`dp[i + 1][j - 1]`，所以 `i` 需要倒序，保证更短区间先被计算。
     - 理解了转移规则：两端相等时包住中间区间，两端不等时跳过左端或右端。

2. **Problem 046: Longest Palindromic Substring (最长回文子串)**
   - **类型**: String / Dynamic Programming / Palindrome
   - **收获记录 / 踩坑反思**:
     - 正确把状态切换成布尔值：`dp[i][j]` 表示 `s[i:j+1]` 是否是回文子串。
     - 明确了回文子串必须连续，不能使用上一题“跳过字符”的转移。
     - 正确维护 `start` 和 `max_len`，最终返回原字符串中的连续切片。
     - 理解了判断回文子串的核心条件：两端字符相等，并且中间区间也是回文。

3. **Problem 047: Count Palindromic Substrings (回文子串计数)**
   - **类型**: String / Dynamic Programming / Palindrome Counting
   - **收获记录 / 踩坑反思**:
     - 在上一题回文子串 DP 的基础上，把目标从“最长答案”切换为“统计所有合法区间”。
     - 正确按位置计数：相同内容但不同位置的回文子串需要分别统计。
     - 使用 `j` 从左到右推进、`i` 从左到右枚举的填表方式，证明了只要依赖项已经算过，遍历顺序可以不止一种。
     - 正确使用 `j - i <= 2` 处理长度 1、2、3 的基础回文区间。

### 今天新增掌握的模式

- **子序列 vs 子串**：
  - 子序列可以跳过字符，但不能改变相对顺序；
  - 子串必须是连续的一段。
- **区间 DP**：用左右边界 `i, j` 描述一个区间，先解决更短区间，再解决更长区间。
- **回文子序列转移**：
  - `s[i] == s[j]` 时，答案来自 `dp[i + 1][j - 1] + 2`；
  - `s[i] != s[j]` 时，答案来自 `max(dp[i + 1][j], dp[i][j - 1])`。
- **回文子串 DP**：
  - `dp[i][j]` 是布尔值，表示整个连续区间是否为回文；
  - 两端相等还不够，中间也必须是回文。
- **DP 遍历顺序判断**：不要死背循环方向，要看当前状态依赖哪些状态，保证依赖项先被计算。

### 综合评价

今天完成了回文字符串 DP 的一组核心训练：最长回文子序列、最长回文子串、回文子串计数。最重要的进步是你主动追问了 `i` 为什么要倒序，以及初始时 `i`、`j` 在区间中的位置。这说明你不只是套模板，而是在理解 DP 表的计算方向。

目前需要继续巩固的是：每道 DP 题先判断状态值到底是什么。Problem 045 的状态是“长度”，Problem 046 和 047 的状态是“是否为回文”。状态值类型一变，转移和答案维护方式都会变。下一次建议继续做字符串 DP，或者把回文子串题用“中心扩展”再做一遍，训练同一问题的不同解法。

---

## 2026年5月24日 训练总结

### 今天完成的题目

1. **Problem 048: Delete Operation for Two Strings (两个字符串的删除操作)**
   - **类型**: String / Dynamic Programming / Longest Common Subsequence
   - **收获记录 / 踩坑反思**:
     - 理解了 LCS 的含义：最长公共子序列是两个字符串都能保留下来的最长有序字符序列，可以不连续，但相对顺序不能改变。
     - 成功把“最少删除次数”转换成“尽量保留最长公共子序列”。
     - 明确公式：`len(word1) + len(word2) - 2 * lcs`。
     - 正确写出二维 LCS DP：字符相等时来自左上角加 1，字符不等时从上方和左方取最大值。

2. **Problem 049: Edit Distance (编辑距离)**
   - **类型**: String / Dynamic Programming / 2D DP
   - **收获记录 / 踩坑反思**:
     - 正确写出经典编辑距离 DP。
     - 明确了 `dp[i][j]` 的含义：把 `word1` 前 `i` 个字符转换成 `word2` 前 `j` 个字符的最少操作次数。
     - 掌握了第一行和第一列初始化：空字符串变成目标前缀需要插入，源前缀变成空字符串需要删除。
     - 初步理解了三个方向对应三种操作：
       - 从上方来：删除 `word1` 当前字符；
       - 从左方来：插入 `word2` 当前字符；
       - 从左上来：替换当前字符。
     - 这题体感偏难，说明“操作型 DP”的状态转移还需要后续多看表格、多画前缀关系来巩固。

3. **Problem 050: Decode Ways (解码方法)**
   - **类型**: String / Dynamic Programming
   - **收获记录 / 踩坑反思**:
     - 成功从二维字符串 DP 切换到一维前缀 DP。
     - 正确定义 `dp[i]` 表示字符串前 `i` 个字符的解码方式数量。
     - 理解了“按最后一步分类”：最后一步可以使用 1 个字符，也可以使用 2 个字符。
     - 正确处理了 `0` 的特殊性：`0` 不能单独解码，只能作为 `10` 或 `20` 的一部分。
     - 可见测试和补充边界测试全部通过。

### 今天新增掌握的模式

- **LCS 转操作次数**：如果题目只允许删除，最终相同的部分一定是公共子序列；为了删除最少，就保留 LCS。
- **二维前缀 DP**：`dp[i][j]` 通常表示两个输入前缀之间的最优值、方案数或最小代价。
- **编辑距离三操作模型**：
  - 删除：看 `dp[i - 1][j]`；
  - 插入：看 `dp[i][j - 1]`；
  - 替换：看 `dp[i - 1][j - 1]`。
- **一维前缀 DP**：当前状态可以来自前一个状态或前两个状态，关键是明确“最后一步用了几个元素”。
- **非法状态处理**：`0`、空字符串、前导零这类边界条件要在初始化和转移中单独判断。

### 综合评价

今天的训练非常扎实：先用 Problem 048 巩固 LCS，再挑战 Problem 049 的经典编辑距离，最后用 Problem 050 回到更轻量的一维 DP。你在编辑距离里感觉“挺难”是正常的，因为这题要求同时理解状态定义、初始化、三种操作和表格方向，是字符串 DP 里比较核心的一关。

比较好的信号是：你不是只满足于代码跑通，而是继续追问 `if/else` 转移为什么这样写、三种操作为什么对应三个不同的 DP 位置。这种追问正是 DP 真正内化的过程。

下一次建议不要急着继续加难度，可以做一到两题一维 DP 或简单二维 DP，把“状态定义 -> 初始化 -> 当前状态从哪里来”这条主线练得更顺。编辑距离可以隔几天后再回头复盘一次，那时会比今天清楚很多。

## 2026年5月25日 训练总结

### 今天完成的题目

1. **Problem 051: Jump Game (跳跃游戏)**
   - **类型**: Array / Greedy
   - **收获记录 / 踩坑反思**:
     - 正确写出贪心解法，可见测试全部通过。
     - 理解了 `farthest` 的含义：当前已经能够到达的最远下标。
     - 理解了更新逻辑 `farthest = max(farthest, i + nums[i])`：
       - `i + nums[i]` 表示站在当前位置时，最远可以跳到哪里。
       - `max(...)` 用来保留历史最远范围，避免可达边界倒退。
     - 理解了判断条件 `farthest >= len(nums) - 1`：最远可达范围已经覆盖最后一个下标，因此可以到达终点。
     - 需要注意变量拼写和提交前清理 TODO 注释。

2. **Problem 052: Jump Game II (跳跃游戏 II)**
   - **类型**: Array / Greedy
   - **收获记录 / 踩坑反思**:
     - 正确写出区间覆盖型贪心解法，可见测试和补充边界测试全部通过。
     - 理解了三个核心变量：
       - `jumps`：已经跳了几次。
       - `current_end`：当前跳跃次数能够覆盖到的最右边界。
       - `farthest`：在当前覆盖范围内，下一跳能够到达的最远位置。
     - 正确使用 `for i in range(len(nums) - 1)`，避免遍历最后一个下标导致多计一次跳跃。
     - 理解了 `i == current_end` 的含义：当前这一层覆盖范围已经扫描完，必须增加一次跳跃，并把覆盖边界扩展到 `farthest`。

### 今天新增掌握的模式

- **可达范围贪心**：不枚举具体跳法，只维护当前所有可达位置中能够延伸出的最远边界。
- **区间层级贪心**：一次跳跃对应一个覆盖区间，扫描完当前区间后才增加跳数。
- **终点边界处理**：判断能否到达时可以看 `farthest >= len(nums) - 1`；计算最少跳数时通常不遍历最后一个下标。

### 综合评价

今天从 Jump Game I 推进到 Jump Game II，重点完成了 Greedy 中“覆盖范围”这一类题型。你先理解了贪心不是枚举路径，而是维护当前最关键的状态；随后能独立写出 `farthest` 更新和区间边界触发跳数的逻辑。第 52 题里最容易错的是最后一个下标多算一次跳跃，你这次处理正确。

下一次建议继续做一题 Greedy 区间题，例如 Gas Station 或 Partition Labels，用不同题面巩固“局部状态推动全局结果”的感觉。

---

## 2026年5月26日 训练总结

### 今天完成的题目

1. **Problem 053: Gas Station (加油站)**
   - **类型**: Array / Greedy
   - **收获记录 / 踩坑反思**:
     - 正确写出 O(n) 贪心解法，可见测试和随机暴力对拍全部通过。
     - 理解了全局条件：如果 `sum(gas) < sum(cost)`，总油量不够，任何起点都不可能完成一圈。
     - 正确维护局部油量 `current_gas`，表示从当前候选起点出发后，到当前位置为止的剩余油量。
     - 理解了贪心关键：当 `current_gas < 0` 时，可以直接把候选起点跳到 `i + 1`，因为当前起点到 `i` 之间的所有位置都不可能作为答案。
     - 对“这题为什么是贪心而不是普通遍历”进行了概念澄清：贪心点不在 for 循环，而在一次性排除一整段不可能起点。

2. **Problem 054: Partition Labels (划分字母区间)**
   - **类型**: String / Hash Map / Greedy
   - **收获记录 / 踩坑反思**:
     - 正确先用哈希表记录每个字符最后一次出现的位置。
     - 正确维护当前片段必须覆盖到的最远下标 `end`。
     - 正确在 `i == end` 时切分片段，并记录片段长度。
     - 理解了贪心关键：当前片段一旦闭合就立刻切分，才能得到尽可能多的片段。
     - 可见测试和随机性质检查全部通过。

### 今天新增掌握的模式

- **全局可行 + 局部重置贪心**：
  - 先判断整体资源是否足够。
  - 再用局部亏空决定是否放弃当前候选方案。
  - 一旦局部状态失败，不是只排除当前起点，而是排除一整段不可能候选。

- **边界闭合切分贪心**：
  - 先把字符约束转换成下标边界。
  - 扫描过程中维护当前片段必须覆盖到的最远位置。
  - 当扫描位置到达边界时，说明当前片段已经完整，可以立刻切分。

- **辨认贪心的角度**：
  - 不是所有带 for 循环的题都是普通遍历。
  - 要看是否存在“当前做出局部决策后，可以安全排除其他选择”的证明。
  - Gas Station 的局部决策是跳过一整段起点；Partition Labels 的局部决策是边界闭合后立即切分。

### 综合评价

今天的重点是把 Greedy 从“代码模板”推进到“为什么可以这么跳过选择”。你在 Gas Station 里主动问“这不就是遍历吗”，这是一个很关键的问题。回答清楚之后，贪心的本质更明确了：不是遍历本身，而是在遍历过程中用一个可证明正确的局部决策减少候选空间。

目前 Greedy 的两个方向已经有基础感觉：

- 覆盖范围类：Jump Game I / II。
- 局部排除或边界闭合类：Gas Station / Partition Labels。

下一次建议继续做一到两题 Greedy，例如 Merge Intervals、Non-overlapping Intervals 或 Assign Cookies，重点练习“排序后如何做局部选择”。

---

## 2026年5月27日 训练总结

### 今天完成的题目

1. **Problem 055: Non-overlapping Intervals (无重叠区间)**
   - **类型**: Array / Sorting / Greedy / Intervals
   - **收获记录 / 踩坑反思**:
     - 正确使用“按区间结束位置排序”的贪心策略。
     - 正确维护 `prev_end`，表示当前已保留区间的结束位置。
     - 正确使用 `start >= prev_end` 判断两个区间是否可以相接但不重叠。
     - 可见测试和随机暴力对拍测试全部通过。
     - 需要注意：`intervals.sort(...)` 会原地修改输入；如果未来题目要求不改变参数，可以改用 `sorted(...)`。

### 今天新增掌握的模式

- **区间调度贪心**：
  - 目标是“删得最少”，可以反过来想成“保留最多的不重叠区间”。
  - 为了给后续区间留下尽可能多的空间，应优先保留结束位置更早的区间。
  - 排序后从左到右扫描，只要当前区间起点不小于已保留区间的结束位置，就可以保留。

- **区间边界判断**：
  - 本题中 `[1, 2]` 和 `[2, 3]` 不算重叠。
  - 因此合法条件是 `start >= prev_end`，不是 `start > prev_end`。

### 综合评价

今天训练量不大，但质量很好：你先主动要求讲解题目和提示，再独立写出标准贪心解法。第 55 题的关键不是循环本身，而是排序依据：按结束位置排序之后，每次保留当前最早结束的合法区间，就能最大化后续可选空间。

目前 Greedy 已经连续覆盖了几个核心形态：

- 覆盖范围：Jump Game I / II。
- 全局可行 + 局部重置：Gas Station。
- 边界闭合切分：Partition Labels。
- 区间调度：Non-overlapping Intervals。

下一次建议继续做一个排序贪心题，例如 Assign Cookies 或 Minimum Number of Arrows to Burst Balloons，重点练习“先排序，再明确当前局部选择到底保留了什么优势”。

---

## 2026年5月29日 训练总结

### 今天完成的题目

1. **Problem 056: Minimum Number of Arrows to Burst Balloons (用最少数量的箭引爆气球)**
   - **类型**: Array / Sorting / Greedy / Intervals
   - **收获记录 / 踩坑反思**:
     - 正确沿用“按区间结束位置排序”的贪心策略。
     - 理解了为什么箭要射在当前未覆盖气球的右边界：在保证射爆当前气球的前提下，尽可能覆盖后面起点更大的气球。
     - 正确维护 `current_arrow_position`，表示当前箭的位置。
     - 正确使用 `start > current_arrow_position` 判断是否需要新增箭。
     - 明确了边界相接的含义：`start == current_arrow_position` 时，当前箭仍然可以射爆该气球。
     - 可见测试和随机暴力对拍测试全部通过。

2. **Problem 057: Merge Intervals (合并区间)**
   - **类型**: Array / Sorting / Intervals
   - **收获记录 / 踩坑反思**:
     - 正确使用“按区间起点排序”的思路。
     - 理解了这题不需要嵌套扫描，也不需要在遍历中删除原列表；排序后只需要线性遍历一次。
     - 正确维护 `merged` 结果列表，并用 `last` 表示当前正在合并的最后一个区间。
     - 正确使用 `last[1] < interval[0]` 判断不重叠，因此边界相接会进入合并逻辑。
     - 正确在重叠时更新右边界：`last[1] = max(last[1], interval[1])`。
     - 可见测试和随机性质测试全部通过。

### 今天补充理解的 Python 语法

- **`list.sort(key=...)`**：
  - `points.sort(key=lambda x: x[1])` 表示对 `points` 原地排序。
  - `key` 指定排序依据，而不是直接按整个元素比较。
  - `x` 表示列表里的每一个元素，例如一个区间 `[start, end]`。
  - `x[1]` 表示取区间的结束位置作为排序依据。

- **`lambda` 匿名函数**：
  - 基本格式是 `lambda 参数: 返回值表达式`。
  - `lambda x: x[1]` 等价于：

```python
def get_end(x):
    return x[1]
```

  - 在排序里经常用来快速指定“按第几个字段排序”。

### 今天新增掌握的模式

- **射气球区间贪心**：
  - 按结束位置排序。
  - 第一支箭放在当前未覆盖气球的结束位置。
  - 如果下一个气球的 `start <= arrow_pos`，说明它被当前箭覆盖。
  - 如果 `start > arrow_pos`，说明必须新增箭，并把新箭放在当前气球的结束位置。

- **区间合并**：
  - 按起点排序。
  - 结果列表 `merged` 的最后一个区间就是当前可能继续扩展的区间。
  - 如果 `current_start <= last_end`，合并并更新 `last_end`。
  - 如果 `current_start > last_end`，说明出现新的独立区间，加入结果。

- **同为区间题，排序依据不同**：
  - Non-overlapping Intervals / Minimum Number of Arrows：按结束位置排序，重点是选择最早结束的区间或射击点。
  - Merge Intervals：按起点排序，重点是从左到右维护当前合并结果。

### 综合评价

今天的重点是区间题连续训练。你已经能从题目目标判断排序依据：需要“选择最优覆盖点或保留区间”时，多考虑按结束位置排序；需要“从左到右合并范围”时，多考虑按起点排序。

今天另一个收获是补上了 `lambda` 和 `sort(key=...)` 的语法理解。这个语法在算法题和工程代码里都非常常见，尤其是二维列表、对象列表、字典项排序。后续遇到排序题时，不要只记模板，要先说清楚：每个元素是什么、排序依据是什么、升序还是降序。

下一次建议继续做 **Insert Interval**。它是 Merge Intervals 的变体，可以训练在已有有序区间中插入新区间，并边扫描边合并。

---

## 2026年5月30日 训练总结

### 今天完成的题目

1. **Problem 058: Insert Interval (插入区间)**
   - **类型**: Array / Intervals
   - **收获记录 / 踩坑反思**:
     - 正确掌握了区间插入的三段式分类：完全在新区间左边、和新区间重叠、完全在新区间右边。
     - 一开始出现结果倒序，根因是“右边区间先进入结果，而合并后的新区间最后才进入结果”。
     - 关键修正是引入 `inserted` 状态，记录合并后的 `new_interval` 是否已经放入 `result`。
     - 明确了边界相接和不相接的区别：`[1, 5]` 与 `[5, 8]` 合并，`[1, 5]` 与 `[6, 8]` 不合并。
     - 可见测试全部通过。

2. **Problem 059: Meeting Rooms II (会议室 II)**
   - **类型**: Array / Sorting / Heap / Intervals
   - **收获记录 / 踩坑反思**:
     - 正式接触最小堆 `heapq`，理解了它的核心作用：快速拿到当前最小值。
     - 正确使用最小堆维护会议室结束时间，用 `rooms[0]` 表示最早空出来的会议室。
     - 正确判断会议室复用条件：`earliest_end <= current_start`。
     - 理解了“按开始时间排序负责推进时间线，最小堆负责管理当前资源状态”。
     - 可见测试全部通过。

### 今天新增掌握的模式

- **区间插入三段式**：左侧区间直接加入结果；重叠区间只扩展新区间；第一次遇到右侧区间时先插入合并后的新区间，再加入右侧区间。
- **插入状态标记**：当一个对象不能立刻确定插入位置时，用布尔变量记录它是否已经进入结果，避免漏插或重复插入。
- **最小堆管理资源**：当问题需要动态找到“最早结束 / 最小值 / 最优先释放的资源”时，用最小堆维护候选状态。

### 综合评价

今天的重点是从区间合并推进到资源分配。Problem 058 暴露出的主要问题不是语法，而是状态顺序：什么时候插入、插入几次、循环结束是否需要补插。Problem 059 则引入了一个新工具最小堆，但你能较快把它落到会议室结束时间这个具体含义上。

下一次建议继续做 1 到 2 道 Heap 入门题，巩固 `heappush`、`heappop` 和 `heap[0]` 的使用，再逐步进入 Top K / K-way merge 类型。

---

## 2026年6月2日 训练总结

### 今天完成的题目

1. **Problem 060: Kth Largest Element in an Array (数组中的第 K 大元素)**
   - **类型**: Heap / Array
   - **收获记录 / 踩坑反思**:
     - 正确使用最小堆维护当前见过的前 `k` 大元素。
     - 理解了堆顶语义：堆顶不是当前最大值，而是当前前 `k` 大元素里最小的那个。
     - 正确使用 `heapq.heapreplace` 在当前数字大于堆顶时替换堆顶。
     - 能正确处理重复元素、负数、`k == 1`、`k == len(nums)` 等边界情况。
     - 代码结果正确，复杂度达到 `O(n log k)`。

2. **Problem 061: Top K Frequent Elements (前 K 个高频元素)**
   - **类型**: Hash Map / Heap / Array
   - **收获记录 / 踩坑反思**:
     - 正确用 dict 手写频率统计，没有依赖 `collections.Counter`。
     - 初版用排序列表模拟堆，结果正确，但没有真正练到 Heap 模式。
     - 修改后正确使用 `heapq.heappush` 和 `heapq.heapreplace` 维护大小为 `k` 的最小堆。
     - 明确了本题比较依据不是数字大小，而是出现频率。
     - 正确把堆元素设计为 `(frequency, num)`，最后返回 `num` 部分。

### 今天新增掌握的模式

- **Top K 最小堆**：
  - 找第 `k` 大或前 `k` 大时，可以维护大小为 `k` 的最小堆。
  - 堆顶表示当前保留候选里“最弱”的那个元素。
  - 新元素只有强于堆顶时，才有资格进入堆。

- **频率表 + Top K 堆**：
  - 先用 dict 把原始数组转换成 `元素 -> 频率`。
  - 再按频率把元素放入大小为 `k` 的最小堆。
  - 堆中保存 `(frequency, num)`，让 Python 的最小堆按频率比较。

- **排序列表不等于堆**：
  - 每次 `sort()` 可以让结果正确，但复杂度更高，也没有练到堆操作。
  - 真正的堆训练要使用 `heapq.heappush`、`heapq.heappop`、`heapq.heapreplace`。

### 今天的代码风格提醒

- `import heapq` 应放在文件顶部。
- 顶层 import 和函数定义之间保留两个空行。
- 顶层函数之间也保留两个空行。
- 注释重点写清楚“不变量”，不要把每一行语法都解释一遍。

### 综合评价

今天正式把 Heap 从“会议室资源管理”推进到了 “Top K” 模式。第 60 题完成得很稳，第 61 题的初版虽然结果正确，但暴露了一个重要区别：能用排序列表做出来，不代表掌握了堆。你后面主动修改成 `heapq` 版本，这一步很关键。

目前 Heap 已经覆盖了两个核心方向：

- 动态资源最小值：Meeting Rooms II。
- Top K 候选维护：Kth Largest / Top K Frequent。

下一次建议继续做 Heap 的第三类经典模式：**多路合并**，例如 `Merge K Sorted Lists` 或数组版本的 `Kth Smallest Element in a Sorted Matrix`。重点练习堆里保存复合状态，例如 `(value, list_index, element_index)`。

---
