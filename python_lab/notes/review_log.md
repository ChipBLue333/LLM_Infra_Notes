# 复盘日志 (Review Log)

记录每道题的复盘信息，包括解题思路、时间/空间复杂度以及注意事项等。
## Problem 001: Two Sum

### 题型
Array / Hash Map

### 我的错误
- 基础实现使用了 $O(n^2)$ 的暴力解法，虽然结果正确，但没有利用哈希表进行优化。

### 正确思路
- 遍历 `nums`。
- 对每个 `num`，检查 `target - num` 是否已经出现在哈希表中。
- 如果出现，直接返回对应下标和当前下标。
- 如果没有，将 `num` 作为键，其下标作为值，存入哈希表。

### 关键模式
- **哈希表（Hash Map）查询优化**：利用空间换时间，将查询的时间复杂度从 $O(n)$ 降为 $O(1)$。

### 复杂度
- Time: $O(n)$ （优化后）
- Space: $O(n)$ （优化后）

### 下次注意
- 当涉及到“在数组中查找配对元素”时，优先考虑使用字典（哈希表）来记录已经扫描过的元素。

## Problem 002: Valid Parentheses

### 题型
String / Stack / Hash Map

### 我的错误
- (无严重错误，一次性 pass)
- 风格瑕疵：使用了 Python 内置关键字 `str` 作为变量名；字典在循环内部重复定义导致不必要的开销；不需要索引时使用了 `enumerate`。

### 正确思路
- 使用列表 `my_list` 模拟栈（Stack）。
- 遇到左括号，入栈。
- 遇到右括号，检查栈是否为空，不为空则弹出栈顶元素。
- 使用字典 `seen_map` 验证弹出的左括号和当前的右括号是否真正成对。
- 全部遍历完后，确认栈是否已经清空。

### 关键模式
- **栈结构 (Stack)**: FILO (先进后出)，由于括号嵌套必然是从最内层开始闭合，非常适合用栈的特性去消除配对。

### 复杂度
- Time: $O(n)$
- Space: $O(n)$ 

### 下次注意
- 不要使用 Python 的保留字 / 内置类型（如 `str`, `list`, `dict`, `int`）作为变量名。
- 常量字典或者不变的映射关系，应当定义在循环外面初始化，避免在循环体内反复创建损耗性能。

## Problem 003: Merge Sorted Array

### 题型
Array / Two Pointers (In-place)

### 我的错误
- (在教练提示下完成，伪代码转换成代码非常准确，没有逻辑错误)

### 正确思路
- 使用三个指针：`p1` 指向 `nums1` 真实末尾，`p2` 指向 `nums2` 末尾，`p` 指向 `nums1` 容量末尾。
- 从后往前比较 `nums1[p1]` 和 `nums2[p2]`，把较大的值填入 `nums1[p]`，然后对应指针往前移。
- 考虑到边界情况：如果 `p1` 先走完，只要把 `nums2` 剩下的数字依次填入最前面即可；如果 `p2` 先走完，说明 `nums1` 剩下的数字本身就有序且在正确位置，无需处理。

### 关键模式
- **双指针 (逆向思维) / Two Pointers (Backward)**: 当需要原地修改且尾部有足够空间时，从后往前填坑可以避免数据被提前覆盖。

### 复杂度
- Time: $O(m + n)$
- Space: $O(1)$

### 下次注意
- 原地修改 (In-place) 时，如果“从前往后”会导致数据被覆盖或产生高昂的移动成本，考虑“从后往前”。

## Problem 004: Best Time to Buy and Sell Stock

### 题型
Array / Dynamic Programming

### 我的错误
- 最初尝试用双层循环和状态标识位来模拟，逻辑复杂且漏掉了正确答案。
- 这之后改用了一层循环，但内部使用了切片 `prices[0:i]` 加 `min()`，导致了 $O(N^2)$ 的隐藏时间复杂度和 $O(N)$ 的由于切片导致的空间复杂度。

### 正确思路
- 只需要一次遍历（One Pass）。
- 动态维护一个 `min_buy_price` 记录迄今为止见过的最低价格。
- 每次用当前价格减去最低价格，尝试更新最大利润。

### 关键模式
- **动态维护历史极值**：不需要反复回头看历史数据，而是随着遍历一天天更新历史状态。

### 复杂度
Time: O(N)
Space: O(1)

### 下次注意
- 注意时间复杂度的隐性退化（不要在 for 循环里用耗时的 python 切片和内置函数 `min()`、`max()` 去扫描长列表）。
- 局部变量不能与函数名冲突（shadowing）。

## Problem 005: Longest Substring Without Repeating Characters

### 题型
Hash Map / Sliding Window / String

### 我的表现
一次性写出最优解！代码一次过，没有任何逻辑漏洞。

### 正确思路
- 使用滑动窗口（双指针 `left` 和 `right`）。
- 使用字典 `char_index` 记录字符最后一次出现的位置，实现 O(1) 的查找和左指针的跳跃。
- 如果遇到的字符属于历史重复字符，并且还在当前窗口内（`char_index[s[right]] >= left`），说明窗口内产生了冲突，直接将 `left` 跨步移动跃过冲突位置。
- 每次移动后更新字符的最新位置，并计算最大长度。

### 关键模式
- **Sliding Window (优化版)**：通常滑动窗口遇到不满足条件时需要使用 `while` 循环让 `left` 一步步向右走直到满足条件。借用哈希表记录位置，可以让 `left` 一步跳跃到位，将时间复杂度严格控制在 $O(N)$ 且常数更小。

### 复杂度
Time: O(N) 
Space: O(Σ) 其中 Σ 表示字符集大小，对于 ASCII 最大为 128 或 256。


## Problem 006: Container With Most Water

### 题型
Array / Two Pointers

### 我的错误
- 无，一次性写出了最优解。

### 正确思路
- 使用双指针，左指针在起始位置，右指针在末尾。
- 每次计算当前区域的盛水量并更新最大值。
- 将指向柱子较短的那个指针向内侧移动，以期找到更高柱子获得更大容量。

### 复杂度
- Time: O(n)
- Space: O(1)

### 下次注意
- 保持这种优秀的双指针思维。

## Problem 007: Minimum Size Subarray Sum

### 题型
Array / Sliding Window

### 我的错误
- 无错误，顺利实现了滑动窗口模式。

### 正确思路
- 使用滑动窗口，右指针不断向右扩张，扩展窗口和。
- 一旦当前窗口和大于等于 target，尝试通过向右移动左指针来缩窄窗口，并在缩窄的过程中更新最小窗口长度。
- 注意指针移动和长度计算的顺序匹配（长度为 `right - left` 是因为 right 提前进了一步）。

### 复杂度
- Time: O(n)
- Space: O(1)

### 下次注意
- Python中可以用 `float('inf')` 来初始化求最小值的变量，比用具体常数（如 `10**5 + 1`）更清晰、也更有扩展性。

## Problem 008: Search Insert Position

### 题型
Array / Binary Search

### 我的表现
- 一次性写出标准二分查找解法，可见测试全部通过。
- 能正确处理 target 存在、应插入中间、应插入头部、应插入尾部、数组长度为 1 等边界情况。

### 正确思路
- 使用 `left` 和 `right` 表示当前搜索区间。
- 每次计算 `mid`，比较 `nums[mid]` 和 `target`。
- 如果找到目标值，直接返回 `mid`。
- 如果目标值更大，说明插入位置在右半部分，移动 `left = mid + 1`。
- 如果目标值更小，说明插入位置在左半部分，移动 `right = mid - 1`。
- 循环结束后，`left` 就是第一个大于等于 `target` 的位置，也就是插入位置。

### 复杂度
- Time: O(log n)
- Space: O(1)

### 下次注意
- 二分题的关键是明确循环结束后指针的含义。本题中 `left` 最终代表插入位置。
- Python 整数不会溢出，但 `mid = left + (right - left) // 2` 是很好的通用写法，值得保留。

## Problem 009: Reverse Linked List

### 题型
Linked List / Pointer

### 我的表现
- 在提示后能正确写出迭代版链表反转。
- 对 `previous`、`current`、`next_node` 三个变量的职责理解到位。
- 可见测试全部通过，包括空链表、单节点链表、双节点链表和多节点链表。

### 正确思路
- 使用 `previous` 表示已经反转好的前半段。
- 使用 `current` 表示当前正在处理的节点。
- 每一轮先保存 `next_node = current.next`，避免断链后丢失后续节点。
- 将 `current.next` 指向 `previous`，完成当前节点的反转。
- 再依次移动 `previous = current`、`current = next_node`。
- 当 `current` 变成 `None` 时，`previous` 就是新的头节点。

### 复杂度
- Time: O(n)
- Space: O(1)

### 下次注意
- 链表题里，修改 `next` 之前先保存后路，这是防止丢节点的关键习惯。
- 可以保留有解释力的注释，但熟练后可以减少逐行注释，让核心步骤自己说话。

## Problem 010: Binary Tree Inorder Traversal

### 题型
Tree Traversal / DFS / Stack

### 我的情况
- 递归版：一开始对 extend 不熟悉，但逻辑全对。
- 迭代/栈版：一次性写对，没有明显错误。

### 正确思路
- **递归版**：拆分为左、根、右三种情况处理。
- **迭代版**：使用栈模拟递归的系统栈。外层循环判断节点不为空或栈不为空；内层循环一路向左把节点压栈，走到底后弹栈访问，然后将当前节点转向右子树重复逻辑。

### 关键模式
- 利用 `while current or stack:` 处理二叉树遍历。
- 这是极其经典的二叉树栈消除递归的模板写法，必须牢记。

### 复杂度
Time: O(N)
Space: O(N)

### 下次注意
- 面试中如果考二叉树遍历，大概率会要求写非递归版本（栈版本）。

## Problem 011: Binary Tree Level Order Traversal

### 题型
Tree Traversal / BFS / Queue

### 我的情况
- 充分理解了 `collections.deque` 的应用。
- 一次性写出了带有内层 size 控制的经典 BFS 层序遍历。
- 对 Python 中 `for _ in range(size)` 的占位符惯用法提出疑问。

### 正确思路
- **BFS 层序模板**：在每次外层 `while queue:` 开始时，先取得当前队列长度 `size = len(queue)`。由于队尾会不断追加下一层的节点，利用一个 `for \_ in range(size)` 的内循环，精准地只从队头弹出当前层的节点。这样就能漂亮地把树拆分成一层一层的结构。

### 关键模式
- 利用 `deque` 做到 $O(1)$ 的出队（`popleft`）。
- **通过提前记录 size 来截断队列的增长，是处理所有涉及“按层/按步”BFS问题的通用解法。**

### 复杂度
Time: O(N)
Space: O(N)

### 下次注意
- `_` 是常用的 throwaway 变量，表示只需要循环次数，不需要具体序号时使用。

## Problem 012: Maximum Depth of Binary Tree

### 题型
Tree Traversal / DFS

### 我的情况
- 选用了递归 (DFS) 的解法。
- 完美理解了“大问题转化为小问题”的递归思维。
- 一次性写对，结构非常清晰。

### 正确思路
- **递归版**：当前树的最大深度 = `max(左子树最大深度, 右子树最大深度) + 1`。边界条件是空树返回 0。

### 关键模式
- 这是树形 DP (Dynamic Programming on Trees) 或后序遍历（Bottom-up）求信息的最基础模板。很多类似求树高、树直径、判断平衡树的问题，都是核心沿用这套逻辑。

### 复杂度
- Time: O(N)
- Space: O(H) (H 为树的高度，最坏 O(N))

### 下次注意
- 代码极其精简干净，继续保持这种思维清晰度！

## Problem 013: Same Tree

### 题型
Tree Traversal / DFS / Recursion

### 我的情况
- 一次性写出正确解法，可见测试全部通过。
- 能正确区分“两棵树都为空”和“只有一棵树为空”这两个关键边界条件。
- 对两棵树同步递归的结构理解到位。

### 正确思路
- 如果 `p` 和 `q` 都是空节点，说明当前子树相同。
- 如果 `p` 和 `q` 只有一个为空，说明结构不同。
- 如果两个节点都存在但值不同，说明节点值不同。
- 当前节点通过后，再递归比较左子树和右子树，两边都相同才返回 `True`。

### 关键模式
- **双树同步递归**：处理两棵树时，每一层都同时检查两个节点的存在性、值，以及对应方向的子树。
- 这是判断相同树、对称树、子树匹配等问题的基础模式。

### 复杂度
- Time: O(N)
- Space: O(H) (H 为树的高度，最坏 O(N))

### 下次注意
- 继续保持先处理空节点边界，再处理当前节点，再递归子问题的顺序。

## Problem 014: Symmetric Tree

### 题型
Tree Traversal / DFS / Recursion

### 我的情况
- 在提示后写出了标准的镜像递归解法。
- 可见测试全部通过。
- 能正确区分 Same Tree 的同方向比较和 Symmetric Tree 的交叉方向比较。

### 正确思路
- 空树是对称树。
- 对非空树，判断 `root.left` 和 `root.right` 是否互为镜像。
- 辅助函数 `is_mirror(left, right)` 中先处理空节点边界：
  - 两个都为空，返回 `True`。
  - 只有一个为空，返回 `False`。
  - 两个节点值不同，返回 `False`。
- 当前节点通过后，递归比较 `left.left` 和 `right.right`，以及 `left.right` 和 `right.left`。

### 关键模式
- **镜像递归 (Mirror Recursion)**：比较两棵树时，不是同方向比较，而是交叉比较。
- 与 Same Tree 的区别：
  - Same Tree: `p.left` vs `q.left`，`p.right` vs `q.right`
  - Symmetric Tree: `left.left` vs `right.right`，`left.right` vs `right.left`

### 复杂度
- Time: O(N)
- Space: O(H) (H 为树的高度，最坏 O(N))

### 下次注意
- 看到“对称”“镜像”“翻转后相同”这类关键词，要第一时间想到交叉递归。

## Problem 015: Path Sum

### 题型
Tree Traversal / DFS / Recursion

### 我的情况
- 一次性写出正确解法，可见测试全部通过。
- 能正确理解“路径必须从根节点到叶子节点”，没有把中间节点误判为合法路径。
- 当前实现先递归左右子树，再在叶子节点处覆盖结果，逻辑正确但控制流略绕。

### 正确思路
- 空树返回 `False`。
- 每到一个节点，就用 `target_sum - root.val` 表示剩余还需要凑出的路径和。
- 如果当前节点是叶子节点，只需要判断 `root.val == target_sum`。
- 如果不是叶子节点，则递归检查左子树或右子树是否存在合法路径。

### 关键模式
- **根到叶路径递归**：递归时携带“剩余目标值”或“当前累计和”。
- 叶子节点是判断答案的关键位置，不能在中间节点提前返回成功。

### 复杂度
- Time: O(N)
- Space: O(H) (H 为树的高度，最坏 O(N))

### 下次注意
- 树递归题里，如果叶子节点是答案判断点，可以先判断叶子，再决定是否继续递归，这样控制流更直接。

## Problem 016: Invert Binary Tree

### 题型
Tree Traversal / DFS / Recursion

### 我的情况
- 一次性写出正确解法，可见测试全部通过。
- 能正确处理空树、单节点、只有左孩子、只有右孩子和完整二叉树等情况。
- 当前写法采用“先递归翻转左右子树，再交换当前节点左右指针”的后序思路，逻辑正确。

### 正确思路
- 空树直接返回 `None`。
- 对每个非空节点，交换它的左孩子和右孩子。
- 然后继续递归翻转交换后的左右子树，或者先递归左右子树再交换当前节点，两种顺序都可以。
- 最后返回原来的 `root`，因为这是原地修改二叉树。

### 关键模式
- **二叉树原地修改递归**：函数既要修改当前节点的指针，也要把同样的操作递归应用到左右子树。
- 这类题的返回值通常还是当前节点本身，用来保持递归调用能够接回父节点。

### 复杂度
- Time: O(N)
- Space: O(H) (H 为树的高度，最坏 O(N))

### 下次注意
- 交换两个变量时可以直接使用 Python 的 tuple unpacking：`root.left, root.right = root.right, root.left`。
- 如果代码已经清楚，避免多余的临时初始化，例如 `temp = None` 后立刻覆盖就没有必要。

## Problem 017: Binary Tree Paths

### 题型
Tree Traversal / DFS / Recursion

### 我的情况
- 一次性写出正确解法，可见测试全部通过。
- 能正确把递归返回值设计成“当前子树所有根到叶路径的字符串列表”。
- 能正确处理空树、单节点、单侧链、负数节点和左右子树都有路径的情况。

### 正确思路
- 空树返回空列表。
- 叶子节点返回只包含当前节点值的列表，例如 `["1"]`。
- 对非叶子节点，分别递归获取左子树路径和右子树路径。
- 把当前节点值作为前缀，加到每条子路径前面，形成完整路径。

### 关键模式
- **递归返回列表并由父节点加工**：子问题只负责返回自己的结果，父节点负责把当前层信息拼接进去。
- 这是树上收集答案类 DFS 的基础模式，后面做 Path Sum II、所有根到叶路径、回溯收集路径都会继续用到。

### 复杂度
- Time: O(N * H) 或 O(total_path_string_length)，因为每条路径字符串拼接需要复制字符。
- Space: O(N * H) 用于结果字符串，递归栈为 O(H)。

### 下次注意
- `left_paths + right_paths` 会创建一个额外列表。数据量小时无所谓，但可以分别遍历左右列表来避免这一步。
- 注释要解释“为什么这么做”，不要重复代码已经表达清楚的内容。

## Problem 018: Path Sum II

### 题型
Tree Traversal / DFS / Recursion / Backtracking

### 我的情况
- 一次性写出正确解法，可见测试全部通过。
- 复用了 Binary Tree Paths 的“子树返回路径列表”模型，并加入了剩余目标值 `target_sum - root.val`。
- 能正确处理空树、单节点命中、单节点不命中、负数路径、多条合法路径和无合法路径。

### 正确思路
- 空树返回空列表。
- 如果当前节点是叶子节点，只有当 `root.val == target_sum` 时才返回 `[[root.val]]`。
- 如果当前节点不是叶子节点，递归到左右子树时把目标值减去当前节点值。
- 子树返回满足剩余目标值的路径后，当前节点把自己的值加到每条路径前面。

### 关键模式
- **剩余目标值递归**：递归时把目标值逐层扣掉，到叶子节点时判断当前节点是否刚好等于剩余目标值。
- **递归返回路径列表**：子树负责返回合法路径，父节点负责给路径加前缀。
- 这题也可以用回溯写法：维护一个共享 `path`，进入节点时 append，离开节点时 pop。

### 复杂度
- Time: O(N * H) 或 O(total_path_length)，因为构造路径列表时会复制路径。
- Space: O(N * H) 用于结果和中间路径，递归栈为 O(H)。

### 下次注意
- `left_paths + right_paths` 会创建额外列表，可以分别遍历左右路径。
- `[root.val] + path` 每次都会创建新列表，这是正确但有额外复制成本的写法；回溯写法能减少中间列表创建，但要小心 append/pop 成对出现。

## Problem 019: Subsets

### 题型
Backtracking / DFS / Array

### 我的情况
- 写出了标准回溯解法，可见测试全部通过。
- 能正确使用 `start` 控制下一层搜索范围，避免重复选择和逆序组合。
- 能正确使用 `path.copy()` 保存当前路径，避免结果列表被后续 `path.pop()` 污染。

### 正确思路
- 使用 `result` 保存所有子集，使用 `path` 保存当前正在构造的子集。
- 每进入一层递归，先把当前 `path` 的副本加入结果。
- 从 `start` 开始遍历候选元素：
  - 选择 `nums[i]`；
  - 递归搜索 `i + 1` 之后的元素；
  - 回溯撤销这次选择。

### 关键模式
- **回溯三步**：做选择、递归、撤销选择。
- **组合型搜索的 start 下标**：下一层只能从当前元素后面继续选，避免重复组合。
- **保存路径时复制**：结果里应保存当前路径的快照，而不是共享的 `path` 对象。

### 复杂度
- Time: O(N * 2^N)
- Space: O(N * 2^N) 用于结果，递归栈和 path 为 O(N)。

### 下次注意
- 空列表不需要单独提前返回，通用回溯逻辑本身就能产生 `[[]]`。
- 注释里注意拼写，例如 `strat` 应为 `start`。

## Problem 020: Permutations

### 题型
Backtracking / DFS / Array

### 我的情况
- 写出了标准的全排列回溯解法，可见测试全部通过。
- 能正确使用 `used` 数组记录当前路径中已经使用过的元素。
- 能在 `len(path) == len(nums)` 时保存完整排列，并及时 `return`。
- 能保持 `path.append()` / `path.pop()` 与 `used[i] = True` / `used[i] = False` 成对出现。

### 正确思路
- 使用 `result` 保存所有完整排列，使用 `path` 保存当前排列前缀。
- 使用 `used` 标记每个下标对应的元素是否已经在当前 `path` 中。
- 每一层都从头遍历所有下标：
  - 如果 `used[i]` 为 `True`，跳过；
  - 否则选择 `nums[i]`，继续递归；
  - 递归返回后撤销选择和使用标记。
- 当 `path` 长度等于 `nums` 长度时，说明一个排列完成。

### 关键模式
- **排列型回溯使用 used**：每一层都可以从全部元素中选择，但同一个路径里不能重复使用同一元素。
- **组合 / 子集使用 start**：下一层只从当前位置之后继续选，避免重复组合。
- **状态成对恢复**：修改了几个状态，回溯时就要恢复几个状态。

### 复杂度
- Time: O(N * N!)
- Space: O(N * N!) 用于结果，递归栈、path 和 used 为 O(N)。

### 下次注意
- 本题约束 `len(nums) >= 1`，所以空数组提前返回不是必要逻辑；如果保留也没问题。
- 函数之间按 PEP 8 建议空两行，例如 `return result` 和 `def normalize` 之间。

## Problem 020: Permutations

### 题型
Backtracking / DFS / Array

### 我的表现
- 准确实现了递归与回溯操作。
- 正确使用了 used 数组防止重复选择。

### 复杂度
- Time: O(n * n!)
- Space: O(n) 用于存储路径和状态

### 下次注意
- 状态保持得非常好。

## Problem 021: Combination Sum

### 题型
Backtracking / DFS / Array

### 我的表现
- 完美！主动对 candidates 进行了排序，有效利用剪枝提前结束不必要的递归分支。
- 逻辑清晰，正确处理了元素的重复使用能力 `backtrack(i, ...)`。

### 复杂度
- Time: O(S), S 为所有可行解的长度之和。最差情况为 O(n^(target/min))。
- Space: O(target/min) 作为隐式的调用栈空间。

### 下次注意
- 同样保持优秀的逻辑，继续挑战有重复元素的去重情况。

## Problem 022: Combination Sum II

### 题型
Backtracking / DFS / Array

### 我的表现
- 完美！准确写出了 `if i > start and candidates[i] == candidates[i - 1]: continue` 这个经典的回溯层级去重逻辑。
- 对剪枝和元素不可多次重用的细节 (`backtrack(i + 1, ...)`) 把握精准。

### 复杂度
- Time: 最差情形 O(2^n)，其中 n 为候选数组长度（所有组合的可能性）。实际运行因剪枝会大幅降低。
- Space: O(n)，用于排序和递归栈。

### 下次注意
- 回溯模块的核心防重、剪枝都掌握得很扎实了。

## Problem 023: Permutations II

### 题型
Backtracking / DFS / Array

### 我的表现
- 完美实现了**显式回溯**（`append` + `pop` 配合 `copy()`）！
- 精准写出了全排列去重最难的判断条件：`if i > 0 and nums[i] == nums[i - 1] and not used[i - 1]`。

### 复杂度
- Time: O(n * n!)，其中 n 是数组的长度（最差情况全部不重复），去重操作大幅优化了重复元素的实际运行时间。
- Space: O(n)，用于 used 数组、递归栈和 path。

### 下次注意
- 显式回溯和隐式回溯的区别已经彻底弄懂。继续保持对状态管理的敏锐度。

## 2026-05-12 训练总结
- **完成进度**: Problem 020 ~ 023 (全排列、组合总和及其去重变种)。
- **核心突破**: 
  1. 彻底攻克了回溯算法中的剪枝和去重技巧。
  2. 深刻理解并掌握了 Python 中的“隐式回溯”（值传递/新建对象）与“显式回溯”（引用传递/状态还原），对内存管理和状态机有了更清晰的认知。
- **评价**: 状态极佳，代码一遍过，表现出了极高的算法工程素养！

## Problem 024: Subsets II

### 题型
Backtracking / DFS / Array

### 我的表现
- 正确沿用了子集题的 `start` 下标模型。
- 能先排序，再使用 `if i > start and nums[i] == nums[i - 1]: continue` 做同层去重。
- 正确使用 `path.append()` / `path.pop()` 显式回溯，并在保存结果时使用 `path.copy()`。

### 正确思路
- 对 `nums` 排序，让相同元素相邻。
- 每进入一层递归，先把当前 `path` 的快照加入结果。
- 从 `start` 开始枚举可选元素：
  - 如果当前元素与前一个元素相同，并且它不是本层第一个候选，跳过；
  - 否则选择当前元素，递归到 `i + 1`；
  - 递归结束后撤销选择。

### 关键模式
- **子集/组合型搜索使用 start**：下一层只能从当前位置之后继续选。
- **同层去重条件**：`i > start and nums[i] == nums[i - 1]`。
- **不要事后去重**：本题应在搜索树生成阶段剪掉重复分支。

### 复杂度
- Time: O(n * 2^n)，最差情况下所有元素互不相同。
- Space: O(n * 2^n) 用于结果，递归栈和 path 为 O(n)。

### 下次注意
- 空数组提前返回可以保留，但通用回溯逻辑本身也能自然产生 `[[]]`。
- 当前函数会原地排序 `nums`，如果不希望修改调用方传入的列表，可以使用 `nums = sorted(nums)`。

## Problem 025: Generate Parentheses

### 题型
Backtracking / DFS / String

### 我的表现
- 正确使用 `left_count` 和 `right_count` 两个状态变量控制搜索。
- 能用 `left_count < n` 判断是否还能添加左括号。
- 能用 `right_count < left_count` 阻止非法括号前缀。
- 使用字符串拼接创建新状态，属于清晰的隐式回溯写法。

### 正确思路
- 从空字符串开始递归构造括号序列。
- 每一步根据当前已使用的左右括号数量决定可选动作：
  - 左括号数量还没到 n，就可以添加 `'('`；
  - 右括号数量小于左括号数量，才可以添加 `')'`；
  - 当前字符串长度达到 `2 * n` 时，保存结果。

### 关键模式
- **约束型回溯**：不是枚举后过滤，而是在生成过程中剪掉非法分支。
- **合法括号前缀条件**：任意时刻 `right_count <= left_count`。
- **隐式回溯**：`current + '('` / `current + ')'` 会创建新字符串，不需要手动撤销状态。

### 复杂度
- Time: O(C_n * n)，其中 C_n 是第 n 个卡特兰数，生成每个长度为 2n 的结果需要 O(n)。
- Space: O(C_n * n) 用于结果，递归深度为 O(n)。

### 下次注意
- 这题的结束条件可以用 `len(current) == 2 * n`，也可以用 `left_count == n and right_count == n`。
- 如果改用共享列表 `path`，就需要恢复到显式回溯：append 后必须 pop。

## Problem 026: Letter Combinations of a Phone Number

### 题型
Backtracking / DFS / String / Hash Map

### 我的表现
- 正确理解了 `index` 表示当前处理 `digits` 的哪个位置。
- 能使用字典保存数字到字母集合的映射。
- 正确使用共享 `path` 列表进行显式回溯，`append` 和 `pop` 成对出现。
- 正确处理了空字符串输入，返回空列表。

### 正确思路
- 每一层递归对应 `digits` 中的一个下标。
- 根据当前数字找到本层可选字母集合。
- 依次选择一个字母加入 `path`，递归处理下一位。
- 当 `index == len(digits)` 时，说明组合完整，保存 `"".join(path)`。

### 关键模式
- **位置驱动的回溯**：递归层数由输入字符串的位置决定。
- **每层候选集合不同**：候选来自 `digit_to_char[digits[index]]`。
- **显式回溯**：共享 `path` 的修改必须恢复。

### 复杂度
- Time: O(4^n * n)，其中 n 是 digits 长度，最坏每位有 4 个候选。
- Space: O(4^n * n) 用于结果，递归栈和 path 为 O(n)。

### 下次注意
- 变量名可以更贴近含义，例如 `digit_to_letters` 比 `digit_to_char` 更准确，因为每个数字对应多个字母。
- 字符串拼接版本也能写，但当前列表加 `join` 的方式更适合训练显式回溯。

## Problem 027: Palindrome Partitioning

### 题型
Backtracking / DFS / String

### 我的表现
- 第一版使用“递归处理剩余字符串”的方式完成了题目，测试通过。
- 根据 review 修改为更标准的 `start + path` 显式回溯结构。
- 能判断当前切出的子串是否为回文串，并只在合法时继续递归。
- 正确使用 `path.append()` / `path.pop()` 恢复状态，并用 `path.copy()` 保存结果快照。

### 正确思路
- 使用 `start` 表示当前从字符串哪个下标开始继续切割。
- 每一层枚举右边界 `j`，得到候选子串 `s[start:j]`。
- 如果候选子串是回文串，就加入 `path` 并递归到 `j`。
- 当 `start == len(s)` 时，说明整条切割路径完成，保存 `path.copy()`。

### 关键模式
- **切割型回溯**：每一层决定当前切一段多长。
- **合法性剪枝**：只有当前切出的子串是回文串，才继续深入。
- **下标驱动**：`start` 控制下一段从哪里开始。
- **显式回溯**：共享 `path` 的修改必须在递归返回后撤销。

### 复杂度
- Time: O(n * 2^n)，最坏情况下需要枚举大量切割方式，每次回文判断和切片都有额外开销。
- Space: O(n * 2^n) 用于结果，递归深度最深为 O(n)。

### 下次注意
- `start >= len(s)` 可以工作，但 `start == len(s)` 更精准。
- 辅助函数参数名不要和外层变量同名，例如用 `text` 替代 `s`。

## Problem 028: Word Search

### 题型
Backtracking / DFS / Matrix

### 我的表现
- 正确把二维搜索建模为 `dfs(i, j, k)`，其中 `k` 表示当前匹配到 `word` 的第几个字符。
- 能先处理越界和字符不匹配，再判断是否已经匹配到最后一个字符。
- 使用原地临时标记 `board[i][j] = "#"` 防止同一路径重复访问。
- 递归搜索上下左右四个方向后，能恢复 `board[i][j]`，回溯状态管理正确。
- 从每个格子尝试作为起点，找到可行路径后立刻返回 `True`。

### 正确思路
- 外层双循环枚举所有起点。
- DFS 当前格子时，先判断当前位置是否合法、字符是否匹配。
- 如果已经匹配到 `word` 的最后一个字符，直接返回成功。
- 否则临时标记当前格子为已访问，再递归搜索四个方向。
- 搜索结束后恢复当前格子，保证其他路径仍然可以使用这个格子。

### 关键模式
- **网格 DFS**：状态通常包含行、列、当前目标下标。
- **原地标记**：可以用临时字符标记访问状态，但递归返回后必须恢复。
- **短路返回**：四个方向中只要有一个成功，就可以立即返回 `True`。
- **路径级访问限制**：格子不能在同一条路径中重复使用，但不同起点、不同路径之间可以重新使用。

### 复杂度
Time: O(m * n * 4^L)，其中 m、n 是网格尺寸，L 是 word 长度。
Space: O(L)，递归深度最多为 word 长度。

### 下次注意
- `m, n` 可以放在 `dfs` 定义之前，可读性更好。
- 原地标记依赖题目保证字符集中没有 `"#"`；如果字符范围不受控，可以改用 `visited` 集合或二维布尔数组。

## Problem 029: Number of Islands

### 题型
DFS / Matrix / Connected Components

### 我的表现
- 正确使用外层双循环遍历所有格子。
- 遇到 `"1"` 时先计数，再用 DFS 把同一座岛屿上的所有陆地标记为 `"0"`。
- DFS 的边界判断、陆地判断和四方向扩展都正确。
- 使用原地修改 grid 的方式避免了额外 visited 结构，代码简洁。

### 正确思路
- 把每个尚未访问的 `"1"` 看作一座新岛屿的入口。
- 每发现一个入口，岛屿数量加 1。
- 从入口出发 DFS，把上下左右连通的所有 `"1"` 都标记为已访问。
- 后续外层遍历再遇到这些位置时，它们已经不是 `"1"`，因此不会重复计数。

### 关键模式
- **连通块计数**：外层遍历负责发现新连通块，DFS/BFS 负责清空当前连通块。
- **网格 DFS**：递归状态只需要当前坐标，不需要像 Word Search 那样携带字符串下标。
- **访问标记**：原地把 `"1"` 改成 `"0"` 是一种常见做法。

### 复杂度
Time: O(m * n)
Space: O(m * n)，最坏情况下整张网格都是陆地，递归栈深度可能达到 m * n。

### 下次注意
- 当前写法会修改输入 grid，这是题目允许的；如果调用方需要保留原网格，应使用 visited 集合或复制 grid。
- Python 默认递归深度有限，极端大网格可能触发 RecursionError；工程场景可以改成 BFS 或显式栈 DFS。

## Problem 030: Rotting Oranges

### 题型
BFS / Queue / Matrix / Multi-source BFS

### 我的表现
- 正确使用 `deque` 作为 BFS 队列。
- 能在初始化阶段同时收集所有腐烂橘子，并统计新鲜橘子数量。
- 正确按层处理队列，每一层代表 1 分钟。
- 能在橘子第一次腐烂时原地标记为 `2`，避免重复入队。
- 正确处理了没有新鲜橘子、没有初始腐烂橘子、无法全部腐烂等边界情况。

### 正确思路
- 先扫描整个网格，把所有初始腐烂橘子作为多源 BFS 的起点加入队列。
- 记录新鲜橘子数量 `fresh_count`。
- 每轮处理当前队列中的所有腐烂橘子，并把上下左右相邻的新鲜橘子变成腐烂橘子。
- 每完成一层扩散，分钟数加 1。
- BFS 结束后，如果 `fresh_count` 仍大于 0，说明存在无法到达的新鲜橘子，返回 -1。

### 关键模式
- **多源 BFS**：所有初始腐烂橘子同时扩散，不是分别从每个源头单独搜索。
- **层序遍历计时**：分钟数应按 BFS 层数增加，而不是按节点数增加。
- **原地访问标记**：把 `1` 改成 `2` 表示已经腐烂并已入队，避免重复处理。

### 复杂度
Time: O(m * n)
Space: O(m * n)，最坏情况下队列中可能包含大量格子。

### 下次注意
- 可以提前保存 `rows = len(grid)` 和 `cols = len(grid[0])`，减少重复计算并提升可读性。
- 如果题目或工程接口不允许修改输入网格，需要复制 grid 或使用 visited 结构。

## Problem 031: Climbing Stairs

### 题型
Dynamic Programming / Math

### 我的表现
- 正确写出了 DP 数组版本。
- 能明确使用 `dp[i] = dp[i - 1] + dp[i - 2]` 作为状态转移。
- 正确初始化 `dp[1] = 1` 和 `dp[2] = 2`。
- 测试全部通过，包括 `n = 1`、`n = 2` 和较大的 `n = 45`。

### 正确思路
- 定义 `dp[i]` 表示爬到第 `i` 阶的方法数。
- 到第 `i` 阶的最后一步只有两种来源：
  - 从第 `i - 1` 阶爬 1 阶；
  - 从第 `i - 2` 阶爬 2 阶。
- 因此 `dp[i] = dp[i - 1] + dp[i - 2]`。
- 从小到大填表，最后返回 `dp[n]`。

### 关键模式
- **一维 DP**：当前状态只依赖前面有限个状态。
- **计数型 DP**：总方法数通常来自多个互斥来源的方法数之和。
- **状态定义优先**：先定义 `dp[i]` 的含义，再写转移方程。

### 复杂度
Time: O(n)
Space: O(n)

### 下次注意
- 题目约束是 `1 <= n <= 45`，所以 `n == 0` 不是必须处理的输入；保留 `n <= 1` 可以工作，但注释要和题目约束一致。
- 当前 `dp[i]` 只依赖前两个状态，后续可以练习用两个变量把空间优化到 O(1)。

## Problem 032: Min Cost Climbing Stairs

### 题型
Dynamic Programming / Array

### 我的表现
- 测试全部通过，能正确处理楼顶不需要额外支付 cost 的关键边界。
- 能使用 `dp` 数组保存阶段性最优解，并用前两个状态推出当前状态。
- 代码逻辑正确，但状态定义的表述和初始化稍有混淆：当前实现更接近“踩到第 i 阶后已经支付的最小成本”，而不是严格的“到达第 i 阶的最小成本”。

### 正确思路
- 一种清晰定义是：`dp[i]` 表示到达第 `i` 阶的最小成本，其中楼顶是第 `len(cost)` 阶。
- 因为可以从第 0 阶或第 1 阶开始，所以 `dp[0] = 0`，`dp[1] = 0`。
- 到达第 `i` 阶有两种来源：
  - 从第 `i - 1` 阶跳 1 阶上来，需要支付 `cost[i - 1]`；
  - 从第 `i - 2` 阶跳 2 阶上来，需要支付 `cost[i - 2]`。
- 因此可以写成 `dp[i] = min(dp[i - 1] + cost[i - 1], dp[i - 2] + cost[i - 2])`。

### 关键模式
- **最小值型 DP**：当前最优值来自若干候选路径中的最小值。
- **状态定义要和初始化一致**：如果 `dp[i]` 表示到达第 i 阶，开始站在第 0 或第 1 阶不需要先付费；如果表示踩到第 i 阶后已付费，则初始值可以是 `cost[0]` 和 `cost[1]`。
- **虚拟终点**：楼顶可以看成第 `n` 阶，但它没有对应的 `cost[n]`。

### 复杂度
Time: O(n)
Space: O(n)

### 下次注意
- DP 题里，代码通过不代表状态定义完全干净；要检查“状态定义、初始值、转移方程”三者是否严格一致。
- 这题有两种常见写法，后续优先练习“到达第 i 阶的最小成本”这种更通用的定义。

## Problem 033: House Robber

### 题型
Dynamic Programming / Array

### 我的表现
- 一次性写出标准 DP 数组解法。
- 能正确处理 `n == 1` 和 `n == 2` 的边界情况。
- 能清楚表达“偷当前房屋”和“不偷当前房屋”两种选择。
- 可见测试全部通过。

### 正确思路
- 定义 `dp[i]` 表示考虑到第 `i` 间房屋时，能偷到的最大金额。
- 如果不偷第 `i` 间，收益是 `dp[i - 1]`。
- 如果偷第 `i` 间，因为不能偷相邻房屋，收益是 `dp[i - 2] + nums[i]`。
- 因此状态转移为 `dp[i] = max(dp[i - 1], dp[i - 2] + nums[i])`。

### 关键模式
- **选择型 DP**：每一步都在“选当前元素”和“不选当前元素”之间取最优。
- **相邻约束**：一旦选择当前元素，就必须跳过前一个元素。
- **一维 DP 空间优化潜力**：当前状态只依赖前两个状态，可以进一步压缩到 O(1) 空间。

### 复杂度
Time: O(n)
Space: O(n)

### 下次注意
- 当前题目约束 `len(nums) >= 1`，所以 `n == 0` 防御式处理可以保留，但不是必需分支。
- 后续可以练习用两个变量维护 `dp[i - 2]` 和 `dp[i - 1]`，把空间复杂度优化到 O(1)。

## Problem 034: Maximum Subarray

### 题型
Dynamic Programming / Array

### 我的表现
- 一次性写出标准 DP 数组解法。
- 能准确使用 `dp[i]` 表示“必须以第 i 个元素结尾的最大连续子数组和”。
- 正确维护 `max_sum` 作为全局最优，避免误返回 `dp[-1]`。
- 可见测试全部通过，包括单元素、全负数、全 0 和最大子数组在中间结束的情况。

### 正确思路
- 定义 `dp[i]` 表示必须以 `nums[i]` 结尾的最大连续子数组和。
- 面对 `nums[i]` 有两个选择：
  - 接在前一个连续子数组后面：`dp[i - 1] + nums[i]`；
  - 从当前位置重新开始：`nums[i]`。
- 因此状态转移为 `dp[i] = max(nums[i], dp[i - 1] + nums[i])`。
- 因为最大子数组不一定以最后一个元素结尾，所以需要遍历过程中维护全局最大值 `max_sum`。

### 关键模式
- **局部最优 DP**：`dp[i]` 不是全局答案，而是带有“必须以 i 结尾”限制的局部状态。
- **全局答案单独维护**：当局部状态不等于最终答案时，需要额外变量记录所有局部状态中的最优值。
- **重新开始 vs 接上前缀**：如果前面的连续和是负贡献，就从当前元素重新开始。

### 复杂度
Time: O(n)
Space: O(n)

### 下次注意
- 当前 `dp[i]` 只依赖 `dp[i - 1]`，可以进一步用一个变量压缩空间到 O(1)。
- 全负数场景下不能把答案初始化为 0；必须从 `nums[0]` 开始初始化。

## Problem 035: Unique Paths

### 题型
Dynamic Programming / Matrix

### 我的表现
- 一次性写出标准二维 DP 解法。
- 正确使用 `dp[i][j]` 表示到达格子 `(i, j)` 的路径数量。
- 正确初始化第一行和第一列为 1。
- 使用列表推导式创建二维数组，避免了 Python 二维列表共享引用的问题。
- 可见测试全部通过，包括单行、单列、`1 x 1` 和普通矩阵。

### 正确思路
- 定义 `dp[i][j]` 表示从左上角走到 `(i, j)` 的路径数量。
- 机器人只能向右或向下移动，所以到达 `(i, j)` 之前只能来自：
  - 上方 `(i - 1, j)`；
  - 左方 `(i, j - 1)`。
- 因此状态转移为 `dp[i][j] = dp[i - 1][j] + dp[i][j - 1]`。
- 第一行只能一直向右走，路径数都是 1；第一列只能一直向下走，路径数也都是 1。

### 关键模式
- **二维 DP 表格填充**：当前位置依赖上方和左方，通常按行从左到右、从上到下填表。
- **边界初始化**：第一行和第一列是转移公式的基础，必须先初始化。
- **矩阵坐标语义**：`i` 表示行，`j` 表示列；返回右下角 `dp[m - 1][n - 1]`。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- Python 中不要用 `[[0] * n] * m` 创建二维数组，否则多行会共享同一个列表。
- 当前状态只依赖上一行和当前行左侧，后续可以练习空间优化到 O(n)。

## Problem 036: Unique Paths II

### 题型
Dynamic Programming / Matrix

### 我的表现
- 正确写出二维 DP 解法。
- 能从 `obstacle_grid` 中提取行数和列数，而不是继续沿用上一题的 `m, n` 输入形式。
- 正确处理第一行和第一列遇到障碍后需要停止初始化的逻辑。
- 正确让障碍格子的路径数保持为 0。
- 可见测试全部通过，包括起点障碍、单格网格、整行阻断和普通矩阵。

### 正确思路
- 定义 `dp[i][j]` 表示从左上角走到格子 `(i, j)` 的路径数量。
- 如果 `(i, j)` 是障碍，`dp[i][j] = 0`。
- 如果 `(i, j)` 不是障碍，且不是第一行或第一列，则只能从上方或左方到达：
  `dp[i][j] = dp[i - 1][j] + dp[i][j - 1]`。
- 第一行和第一列不能无脑初始化为 1；一旦遇到障碍，后面的格子都无法继续从起点直线到达。

### 关键模式
- **二维 DP + 阻断条件**：障碍格子直接切断路径贡献。
- **边界初始化要受输入影响**：第一行和第一列是否可达取决于障碍位置。
- **输入格式变化**：上一题输入尺寸 `m, n`，本题输入完整网格，需要从网格中推导尺寸。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- `return dp[r - 1][c - 1]` 比 `dp[-1][-1]` 更适合训练阶段，因为它明确对应右下角坐标。
- 当前二维 DP 正确；后续可以练习用一维数组把空间优化到 O(n)。

## Problem 037: Minimum Path Sum

### 题型
Dynamic Programming / Matrix

### 我的表现
- 正确写出二维 DP 解法。
- 能清楚定义 `dp[i][j]` 表示到达格子 `(i, j)` 的最小路径和。
- 正确初始化第一行和第一列，因为边界格子只能从一个方向到达。
- 正确使用 `min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j]` 进行状态转移。
- 发现了测试用例期望值错误，没有盲目信任测试，这是很重要的工程判断。

### 正确思路
- 定义 `dp[i][j]` 表示从左上角到 `(i, j)` 的最小路径和。
- 起点初始化为 `dp[0][0] = grid[0][0]`。
- 第一行只能从左边来，所以不断累加左侧路径和。
- 第一列只能从上方来，所以不断累加上方路径和。
- 普通格子可以从上方或左方到达，选择代价更小的来源。

### 关键模式
- **最小值型二维 DP**：当前状态来自若干候选来源中的最小值。
- **路径数量 vs 路径代价**：路径数量题通常相加，路径代价最优题通常取 `min` 或 `max`。
- **测试也需要验证**：当实现逻辑自洽但测试失败时，要检查 expected value 是否正确。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- 提交前删除调试输出，例如 `print(dp)`。
- 当前二维 DP 正确；后续可以练习空间优化到 O(n)，或在允许修改输入时原地更新。

## Problem 038: Triangle

### 题型
Dynamic Programming / Array

### 我的表现
- 正确写出二维 DP 解法。
- 能明确 `dp[i][j]` 表示到达三角形中 `(i, j)` 位置的最小路径和。
- 正确处理每一行第一个元素和最后一个元素的边界来源。
- 正确使用最后一行的最小值作为答案，而不是固定返回最后一个元素。
- 可见测试全部通过，包括单元素、负数、全 0 和普通三角形。

### 正确思路
- 顶部初始化为 `dp[0][0] = triangle[0][0]`。
- 对于第 `i` 行第 `j` 个位置：
  - 如果 `j == 0`，只能从上一行同列 `j` 来。
  - 如果 `j == i`，只能从上一行 `j - 1` 来。
  - 否则可以从上一行 `j - 1` 或 `j` 来，选择较小路径和。
- 最终路径可以落在最后一行任意位置，所以答案是 `min(dp[-1])`。

### 关键模式
- **不规则二维 DP**：每一行长度不同，不能照搬矩形网格的列数。
- **边界来源分开处理**：左边界、右边界和中间位置的状态转移不同。
- **答案位置不固定**：不是所有路径类 DP 的答案都在右下角，要根据题意判断终点集合。

### 复杂度
Time: O(n^2)，其中 n 是三角形行数
Space: O(n^2)

### 下次注意
- `# TODO: implement here` 在完成后可以删除。
- 行内注释偏长时，可以拆成上一行注释，避免代码横向拉太长。
- 后续可以练习自底向上 DP 或一维数组空间优化。

## Problem 039: Coin Change

### 题型
Dynamic Programming / Complete Knapsack

### 我的表现
- 正确写出一维 DP 解法。
- 能明确 `dp[x]` 表示凑出金额 `x` 所需的最少硬币数。
- 正确使用 `amount + 1` 作为不可达状态。
- 正确初始化 `dp[0] = 0`。
- 正确通过 `dp[x - coin] + 1` 更新当前金额的最少硬币数。
- 可见测试全部通过，包括不可达金额、金额为 0、普通组合和较大用例。

### 正确思路
- 建立长度为 `amount + 1` 的 dp 数组。
- `dp[0] = 0`，表示凑出金额 0 不需要任何硬币。
- 其他位置初始化为 `amount + 1`，表示当前不可达。
- 枚举金额 `x`，再枚举每个硬币 `coin`。
- 如果 `coin <= x`，尝试从 `dp[x - coin] + 1` 转移到 `dp[x]`。
- 最后如果 `dp[amount]` 仍然是不可达状态，返回 -1；否则返回 `dp[amount]`。

### 关键模式
- **最少数量型 DP**：状态值记录最优数量，转移时取 `min`。
- **不可达状态**：用一个不可能成为合法答案的哨兵值表示还不能凑出该金额。
- **完全背包基础形态**：每种硬币可以重复使用，因此当前金额可以依赖更小金额的最优解。

### 复杂度
Time: O(amount * len(coins))
Space: O(amount)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 行内注释过长时，拆到上一行，保持代码宽度可读。
- 后续要区分“求最少硬币数”和“求组合数量”，两类 Coin Change 的 dp 含义和循环顺序会不同。

## Problem 040: Coin Change II

### 题型
Dynamic Programming / Complete Knapsack

### 我的表现
- 正确写出一维 DP 解法。
- 能明确 `dp[x]` 表示凑出金额 `x` 的组合数量。
- 正确初始化 `dp[0] = 1`，表示不选择任何硬币这一种方案。
- 正确使用“先遍历硬币，再遍历金额”的循环顺序，避免把不同顺序的同一组合重复计算。
- 正确使用 `dp[x] += dp[x - coin]` 累加方案数。
- 单题测试和全量测试全部通过。

### 正确思路
- `dp[x]` 表示在当前已经处理过的硬币范围内，凑出金额 `x` 的组合数量。
- 初始化 `dp[0] = 1`，因为金额 0 有一种空组合。
- 外层遍历硬币，保证组合按照硬币种类逐步扩展，不重复计算顺序不同但内容相同的组合。
- 内层金额从 `coin` 到 `amount` 正向遍历，允许当前硬币重复使用。
- 对于每个金额 `x`，`dp[x - coin]` 表示加入当前硬币之前，能凑出剩余金额的方案数量。
- 因为是在统计总方案数，所以要累加到 `dp[x]`，而不是覆盖 `dp[x]`。

### 关键模式
- **方案数量型 DP**：状态值表示方案数，转移通常用加法累积。
- **组合 vs 排列**：组合题通常先遍历物品，再遍历容量；排列题通常先遍历容量，再遍历物品。
- **完全背包正向遍历**：金额从小到大遍历，使同一硬币可以被重复使用。

### 复杂度
Time: O(amount * len(coins))
Space: O(amount)

### 下次注意
- `# TODO: implement here` 完成后删除。
- 对 `dp[x] += dp[x - coin]` 的理解要巩固：这是把“使用当前硬币形成的新方案数”加到已有方案数里。
- 如果写成 `dp[x] = dp[x - coin]`，会覆盖掉之前硬币已经形成的方案，导致少算。

## Problem 041: Partition Equal Subset Sum

### 题型
Dynamic Programming / 0-1 Knapsack

### 我的表现
- 正确将问题转换为：是否存在一个子集，其和等于总和的一半。
- 正确处理总和为奇数时直接返回 False。
- 能明确 `dp[x]` 表示是否可以用已经遍历过的数字凑出和 `x`。
- 正确初始化 `dp[0] = True`。
- 正确使用倒序遍历容量，避免同一个数字在一轮中被重复使用。
- 单题测试和全量测试全部通过。

### 正确思路
- 先计算数组总和 `total`。
- 如果 `total` 是奇数，不可能分成两个和相等的子集。
- 否则令 `target = total // 2`。
- 使用一维布尔 DP：`dp[x]` 表示能否凑出和 `x`。
- 对每个数字 `num`，从 `target` 倒序遍历到 `num`。
- 如果之前能凑出 `x - num`，那么加入当前数字后就可以凑出 `x`。
- 最终返回 `dp[target]`。

### 关键模式
- **0/1 背包**：每个元素只能使用一次。
- **倒序遍历容量**：防止当前数字在同一轮更新中被重复使用。
- **可达性 DP**：状态值是布尔值，转移使用 `or`。

### 复杂度
Time: O(n * target)
Space: O(target)

### 下次注意
- `# TODO: implement here` 完成后删除。
- 注释可以继续保留，但训练后期要逐渐从“解释每一步”过渡到“解释关键决策”。
- 继续区分完全背包和 0/1 背包：完全背包通常正序容量，0/1 背包通常倒序容量。

## Problem 042: Target Sum

### 题型
Dynamic Programming / 0-1 Knapsack / Count Ways

### 我的表现
- 正确将 `+/-` 符号问题转换为子集和计数问题。
- 能推导出加号组容量 `positive = (sum(nums) + target) // 2`。
- 正确处理 `(sum(nums) + target)` 为奇数时无解。
- 正确处理 `positive < 0` 的情况。
- 能明确 `dp[x]` 表示凑出和 `x` 的方案数。
- 正确使用倒序遍历容量，保证每个数字只使用一次。
- 正确处理数组中包含 `0` 的情况，方案数会翻倍。
- 单题测试和全量测试全部通过。

### 正确思路
- 设加号组数字和为 `positive`，减号组数字和为 `negative`。
- 根据题意有 `positive - negative = target`。
- 因为每个数字必须使用一次，所以有 `positive + negative = sum(nums)`。
- 两式相加得到 `positive = (sum(nums) + target) / 2`。
- 如果 `sum(nums) + target` 是奇数，或者得到的 `positive` 小于 0，则不存在合法选择。
- 使用一维 DP：`dp[x]` 表示从已经遍历过的数字中选若干个，凑出和 `x` 的方案数。
- 初始化 `dp[0] = 1`，表示凑出 0 有一种空选择。
- 对每个数字 `num`，从 `positive` 倒序遍历到 `num`，执行 `dp[x] += dp[x - num]`。

### 关键模式
- **符号选择转子集和**：把 `+/-` 分组问题转换为选出加号组。
- **方案数 0/1 背包**：状态值是方案数量，转移使用累加。
- **倒序容量遍历**：每个元素只能使用一次。
- **0 的特殊性**：选择 `+0` 或 `-0` 表达式值相同，但符号方案不同，因此方案数翻倍。

### 复杂度
Time: O(n * positive)
Space: O(positive)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 可以把非法情况判断放在计算 `positive` 之前，用 `total_plus_target = s + target` 让意图更清楚。
- 注释重点保留公式推导和倒序遍历原因即可，普通赋值不需要解释太多。

## Problem 043: Longest Common Subsequence

### 题型
Dynamic Programming / String / 2D DP

### 我的表现
- 正确写出二维 DP 解法。
- 能明确 `dp[i][j]` 表示 `text1` 的前 `i` 个字符和 `text2` 的前 `j` 个字符的最长公共子序列长度。
- 正确将 DP 表开成 `(m + 1) * (n + 1)`，用第 0 行和第 0 列自然表示空字符串。
- 正确处理字符串下标和 DP 下标之间差 1 的关系。
- 当前字符相等时，正确使用 `dp[i - 1][j - 1] + 1`。
- 当前字符不相等时，正确使用 `max(dp[i - 1][j], dp[i][j - 1])`。
- 单题测试和补充边界测试全部通过。

### 正确思路
- 令 `m = len(text1)`，`n = len(text2)`。
- 创建二维表 `dp`，其中 `dp[i][j]` 表示两个前缀 `text1[:i]` 和 `text2[:j]` 的 LCS 长度。
- 初始化时，第 0 行和第 0 列都为 0，因为任意字符串和空字符串的 LCS 长度都是 0。
- 遍历 `i` 从 1 到 `m`，遍历 `j` 从 1 到 `n`。
- 如果 `text1[i - 1] == text2[j - 1]`，说明当前两个字符可以一起加入公共子序列，转移为 `dp[i - 1][j - 1] + 1`。
- 如果两个字符不相等，说明不能同时选这两个末尾字符，转移为去掉其中一个末尾字符后的较大值。
- 最终答案是 `dp[m][n]`。

### 关键模式
- **二维前缀 DP**：用 `i` 和 `j` 分别表示两个输入的前缀长度。
- **多开一行一列**：简化空前缀边界，避免额外判断。
- **相等看左上，不等看上和左**：LCS 的核心转移模式。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 注释写得很清楚，后续可以逐渐压缩为“状态定义 + 转移原因”两类关键注释。
- 文件名以数字开头时，不能用普通 `from problems.043_xxx import ...` 语法导入；需要直接运行文件或用 `importlib`。

## Problem 044: Uncrossed Lines

### 题型
Dynamic Programming / Array / 2D DP / Longest Common Subsequence

### 我的表现
- 正确识别出“不相交线”等价于两个数组中的匹配下标序列都单调递增。
- 正确将问题转换为数组版本的最长公共子序列。
- 能明确 `dp[i][j]` 表示 `nums1` 的前 `i` 个元素和 `nums2` 的前 `j` 个元素最多能画多少条不相交线。
- 正确使用 `(m + 1) * (n + 1)` 的二维 DP 表处理空数组边界。
- 当前数字相等时，正确使用 `dp[i - 1][j - 1] + 1`。
- 当前数字不相等时，正确使用 `max(dp[i - 1][j], dp[i][j - 1])`。
- 单题测试和补充边界测试全部通过。

### 正确思路
- 不相交的本质是：如果一条线连接 `(i1, j1)`，另一条线连接 `(i2, j2)`，当 `i1 < i2` 时，必须有 `j1 < j2`。
- 因此选出的匹配数字必须在两个数组中都保持原始相对顺序。
- 这正是最长公共子序列问题，只是元素从字符变成了整数。
- 令 `dp[i][j]` 表示 `nums1[:i]` 和 `nums2[:j]` 的最多不相交连接数。
- 如果 `nums1[i - 1] == nums2[j - 1]`，这两个当前末尾数字可以连接，答案来自左上角加 1。
- 如果当前两个数字不相等，就分别尝试跳过 `nums1` 当前数字或跳过 `nums2` 当前数字，取两者较大值。

### 关键模式
- **不相交线转 LCS**：线不交叉等价于匹配顺序一致。
- **二维前缀 DP**：用两个维度分别控制两个数组的已处理前缀。
- **相等连接，不等跳过**：相等时取左上加 1，不等时取上方和左方的较大值。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 行内注释有点长时，可以拆成上一行注释，代码会更清爽。
- 不要比较单条线内部的 `i` 和 `j` 大小；判断是否相交要比较两条线之间的相对顺序。

## Problem 045: Longest Palindromic Subsequence

### 题型
Dynamic Programming / String / Interval DP

### 我的表现
- 正确区分了最长回文子序列和最长回文子串。
- 正确使用 `dp[i][j]` 表示区间 `s[i:j+1]` 内的最长回文子序列长度。
- 正确初始化 `dp[i][i] = 1`，表示单个字符本身是回文。
- 正确使用从右往左枚举 `i`、从左往右枚举 `j` 的填表顺序，保证依赖的更短区间已经计算完成。
- 当 `s[i] == s[j]` 时，正确使用 `dp[i + 1][j - 1] + 2`。
- 当 `s[i] != s[j]` 时，正确使用 `max(dp[i + 1][j], dp[i][j - 1])`。
- 单题测试和补充暴力交叉验证测试全部通过。

### 正确思路
- 区间 DP 的核心是先解决更短区间，再解决更长区间。
- `dp[i][j]` 表示闭区间 `[i, j]` 内最长回文子序列长度。
- 如果左右端点字符相同，左右端点可以一起进入某个回文子序列，中间部分来自 `dp[i + 1][j - 1]`。
- 如果左右端点字符不同，不可能同时把这两个端点作为同一个回文子序列的两端，只能跳过左端点或跳过右端点。
- 最终答案是 `dp[0][n - 1]`。

### 关键模式
- **区间 DP**：状态由左右边界 `i, j` 定义。
- **先短后长**：填表顺序必须保证 `dp[i + 1][j - 1]`、`dp[i + 1][j]`、`dp[i][j - 1]` 已经存在。
- **相等包两边，不等丢一边**：这是最长回文子序列的核心转移。

### 复杂度
Time: O(n^2)
Space: O(n^2)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 行内注释可以再短一点，把“为什么这样转移”写清楚即可。
- 当前代码依赖题目约束 `len(s) >= 1`；如果以后函数要做成通用工具，需要额外考虑空字符串。

## Problem 046: Longest Palindromic Substring

### 题型
String / Dynamic Programming / Palindrome

### 我的表现
- 正确区分了回文子串和回文子序列：本题返回连续子串本身。
- 正确使用 `dp[i][j]` 表示 `s[i:j+1]` 是否是回文子串，状态值是布尔值。
- 正确初始化 `dp[i][i] = True`，表示单个字符是回文。
- 正确使用从右往左枚举 `i`、从左往右枚举 `j` 的填表顺序，保证 `dp[i + 1][j - 1]` 已经计算完成。
- 正确处理长度为 2 的回文子串：当 `j - i == 1` 且两端字符相等时直接成立。
- 正确维护 `start` 和 `max_len`，最终返回原字符串中的连续切片。
- 单题测试和补充暴力枚举交叉验证全部通过。

### 正确思路
- `dp[i][j]` 表示闭区间 `[i, j]` 是否整体构成回文子串。
- 如果 `s[i] != s[j]`，两端不同，当前区间一定不是回文。
- 如果 `s[i] == s[j]`，还需要判断中间区间 `s[i+1:j]` 是否为回文。
- 对长度为 2 的区间，中间为空，只要两端相等就是回文。
- 每次发现新的回文区间时，用区间长度更新当前最长答案。

### 关键模式
- **回文子串 DP**：状态是布尔值，不是长度。
- **两端相等 + 中间回文**：这是判断连续回文子串的核心。
- **返回切片**：用 `start` 和 `max_len` 保存答案位置，保证返回的是原字符串连续片段。

### 复杂度
Time: O(n^2)
Space: O(n^2)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 可以把很长的行内注释拆成独立注释，避免代码行过长。
- 如果使用 `j - i <= 2` 写法，可以统一处理长度 2 和长度 3 的基础区间。

## Problem 047: Count Palindromic Substrings

### 题型
String / Dynamic Programming / Palindrome Counting

### 我的表现
- 正确使用 `dp[i][j]` 表示 `s[i:j+1]` 是否是回文子串。
- 正确把状态值设计为布尔值，并用额外的 `count` 统计答案。
- 正确处理长度为 1、2、3 的基础区间：当 `j - i <= 2` 且两端字符相等时直接成立。
- 正确处理更长区间：两端字符相等时，依赖 `dp[i + 1][j - 1]`。
- 使用了按右边界 `j` 从左到右推进的填表顺序，能够保证 `dp[i + 1][j - 1]` 已经在上一列计算完成。
- 正确按位置计数，相同内容但不同位置的回文子串会被分别统计。
- 单题测试和补充暴力枚举交叉验证全部通过。

### 正确思路
- 遍历所有连续区间 `[i, j]`。
- 如果 `s[i] != s[j]`，当前区间一定不是回文。
- 如果 `s[i] == s[j]`，短区间直接成立，长区间取决于中间区间是否回文。
- 每当 `dp[i][j]` 为 `True`，说明发现了一个以具体位置 `[i, j]` 表示的回文子串，计数加 1。

### 关键模式
- **回文子串计数**：不是维护最长答案，而是每个合法区间都计一次。
- **位置敏感计数**：相同字符串内容出现在不同位置，也算不同子串。
- **按右边界填表**：`j` 从小到大时，`dp[i + 1][j - 1]` 属于更早的右边界，已经计算完成。

### 复杂度
Time: O(n^2)
Space: O(n^2)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 注释里“长度为1或2”这一句可以更精确地写成“长度不超过3”。
- 这题也可以用中心扩展做到 O(1) 额外空间，后续可以作为同题二解练习。

## Problem 048: Delete Operation for Two Strings

### 题型
String / Dynamic Programming / Longest Common Subsequence

### 我的表现
- 正确识别出本题可以转化为最长公共子序列问题。
- 正确使用 `dp[i][j]` 表示 `word1` 前 `i` 个字符和 `word2` 前 `j` 个字符的 LCS 长度。
- 正确处理字符相等和不相等时的状态转移。
- 正确用 `len(word1) + len(word2) - 2 * lcs` 计算最少删除次数。
- 单题测试全部通过。

### 正确思路
- 两个字符串只能通过删除变成相同字符串，因此最终保留下来的部分一定是二者的公共子序列。
- 为了让删除次数最少，需要保留尽可能长的公共子序列，也就是 LCS。
- 如果 LCS 长度为 `lcs`，那么 `word1` 需要删除 `len(word1) - lcs` 个字符，`word2` 需要删除 `len(word2) - lcs` 个字符。

### 关键模式
- **删除操作转 LCS**：只允许删除时，最优目标是最大化共同保留部分。
- **二维前缀 DP**：多开一行一列，让空前缀自然作为边界状态。
- **长度反推操作数**：先求可保留的最大长度，再用总长度减去保留长度得到最少删除次数。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- `L1`、`L2` 可以写成 `len1`、`len2` 或 `m`、`n`，命名风格会更常见。
- 后续可以尝试空间优化版，把二维 DP 压缩成一维数组。

## Problem 049: Edit Distance

### 题型
String / Dynamic Programming / 2D DP

### 我的表现
- 正确写出了编辑距离的二维 DP 解法。
- 正确定义 `dp[i][j]`：将 `word1` 前 `i` 个字符转换成 `word2` 前 `j` 个字符的最少操作次数。
- 正确初始化第一列和第一行，分别对应删除到空字符串、从空字符串插入字符。
- 正确处理当前字符相等时不增加操作次数。
- 正确处理当前字符不相等时的三种选择：删除、插入、替换。
- 单题测试和补充边界测试全部通过。

### 正确思路
- `dp[i][j]` 表示前缀转换的最小代价。
- 如果 `word1[i - 1] == word2[j - 1]`，当前字符可以直接对齐，继承 `dp[i - 1][j - 1]`。
- 如果当前字符不同，需要从三种操作中选择代价最小的一种：
  - 删除 `word1` 当前字符：`dp[i - 1][j] + 1`
  - 插入 `word2` 当前字符：`dp[i][j - 1] + 1`
  - 替换当前字符：`dp[i - 1][j - 1] + 1`

### 关键模式
- **编辑距离 DP**：二维状态表示两个前缀之间的最小转换代价。
- **三操作对应三方向**：删除看上方，插入看左方，替换看左上方。
- **空前缀初始化**：第一行和第一列不是默认值，而是实际操作次数。

### 复杂度
Time: O(m * n)
Space: O(m * n)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 注释里有一个小 typo：`dpp[i][j]` 应改为 `dp[i][j]`。
- 第一行/第一列的中文注释和循环变量可以更精确对应，避免以后复习时读反。

## Problem 050: Decode Ways

### 题型
String / Dynamic Programming

### 我的表现
- 正确写出一维 DP 解法。
- 正确定义 `dp[i]` 表示 `s` 的前 `i` 个字符的解码方式数量。
- 正确初始化 `dp[0] = 1` 和根据首字符处理 `dp[1]`。
- 正确区分一位解码和两位解码两种来源，并把方案数累加。
- 正确处理字符 `'0'`：不能单独解码，只能作为 `10` 或 `20` 的一部分。
- 单题测试和补充边界测试全部通过。

### 正确思路
- 对每个位置 `i`，考虑最后一步解码用了几个字符。
- 如果 `s[i - 1] != '0'`，最后一个字符可以单独解码，继承 `dp[i - 1]`。
- 如果 `s[i - 2:i]` 在 `10` 到 `26` 之间，最后两个字符可以一起解码，继承 `dp[i - 2]`。
- 两种来源互不冲突，因此使用累加得到 `dp[i]`。

### 关键模式
- **一维前缀 DP**：当前状态来自前一个状态和前两个状态。
- **按最后一步分类**：最后一步用 1 个字符或 2 个字符。
- **非法零处理**：`0` 不能独立贡献方案数。

### 复杂度
Time: O(n)
Space: O(n)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 行内注释前建议保留两个空格，符合常见 Python 风格。
- 后续可以尝试用两个变量把空间优化到 O(1)。

## Problem 051: Jump Game

### 题型
Array / Greedy

### 我的表现
- 正确使用贪心思路，没有递归枚举所有跳法。
- 正确维护“当前最远可达下标”。
- 正确判断 `i > farthest` 时当前位置不可达，应直接返回 `False`。
- 正确用 `max(farthest, i + nums[i])` 扩展可达范围。
- 单题测试全部通过。

### 正确思路
- 遍历数组下标 `i`。
- 如果 `i` 已经超过当前最远可达位置，说明无法走到当前位置，后续也不可能继续推进。
- 如果 `i` 可达，就用当前位置的跳跃能力更新最远可达下标。
- 遍历结束后，判断最远可达位置是否覆盖最后一个下标。

### 关键模式
- **覆盖范围贪心**：不关心具体跳法，只关心当前能覆盖到的最远边界。
- **不可达提前失败**：一旦发现当前位置超过最远可达位置，可以直接返回。
- **局部扩展全局范围**：每个可达位置都尝试扩大整体可达区间。

### 复杂度
Time: O(n)
Space: O(1)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 变量名 `fartherest` 拼写不准确，建议改成 `farthest`。
- 这题可以在 `farthest >= len(nums) - 1` 时提前返回 `True`，但不是必须。

## Problem 052: Jump Game II

### 题型
Array / Greedy

### 我的表现
- 正确使用贪心区间扩展思路。
- 正确维护 `jumps`、`current_end`、`farthest` 三个核心变量。
- 正确只遍历到倒数第二个下标，避免到达终点后多计一次跳跃。
- 正确在 `i == current_end` 时增加跳数，并把当前覆盖边界扩展到 `farthest`。
- 单题测试和补充边界测试全部通过。

### 正确思路
- `current_end` 表示当前跳跃次数能够覆盖到的最右边界。
- `farthest` 表示在当前覆盖范围内，下一跳能够到达的最远下标。
- 遍历当前覆盖范围时持续更新 `farthest`。
- 当遍历到 `current_end`，说明当前这一层范围已经看完，必须增加一次跳跃，并进入下一层范围。

### 关键模式
- **区间层级贪心**：一次跳跃对应一个可覆盖区间。
- **边界触发计数**：只有走到当前区间右边界时，才增加跳数。
- **不遍历终点**：到达最后一个下标后不需要再从终点起跳。

### 复杂度
Time: O(n)
Space: O(1)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- `return jumps` 后面有多余空格，提交前清理。
- 注释已经能帮助理解，但后续可以逐渐减少“逐行翻译式注释”，保留变量含义和关键边界即可。

## Problem 053: Gas Station

### 题型
Array / Greedy

### 我的表现
- 正确使用总汽油量与总消耗量判断全局是否可行。
- 正确维护从当前候选起点出发的局部油量 `current_gas`。
- 正确在局部油量变成负数时，把候选起点移动到 `i + 1`，并重置局部油量。
- 单题测试和随机暴力对拍测试全部通过。

### 正确思路
- 如果 `sum(gas) < sum(cost)`，总油量不够，任何起点都不可能完成一圈。
- 如果总油量足够，那么一定存在可行起点。
- 从当前候选起点出发，如果走到 `i` 时局部油量为负，说明当前起点不可行。
- 同时，当前候选起点到 `i` 之间的所有位置也都不能作为答案，因此下一个候选起点应直接设为 `i + 1`。

### 关键模式
- **全局可行 + 局部重置**：先判断整体资源是否够，再用局部亏空决定起点迁移。
- **亏空区间整体排除**：一段路从起点走到 `i` 已经亏空，则这段中的任意点都无法作为真正起点。
- **单次遍历贪心**：不模拟每个起点绕圈，而是在一次扫描中排除不可能区间。

### 复杂度
Time: O(n)
Space: O(1)

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 清理 `return -1` 和 `return start` 后面的多余空格。
- 注释可以减少到关键不变量，例如 `current_gas` 的含义和亏空时为什么跳到 `i + 1`。

## Problem 054: Partition Labels

### 题型
String / Hash Map / Greedy

### 我的表现
- 正确先用哈希表记录每个字符最后一次出现的位置。
- 正确维护当前片段必须覆盖到的最远下标 `end`。
- 正确在 `i == end` 时切分片段，并记录片段长度。
- 单题测试和随机性质检查全部通过。

### 正确思路
- 如果某个片段包含字符 `c`，这个片段必须覆盖到 `c` 最后一次出现的位置。
- 从左到右扫描时，当前片段的右边界应为片段内所有字符最后出现位置的最大值。
- 当扫描下标 `i` 到达当前右边界 `end` 时，说明当前片段内所有字符都已经被完整覆盖，可以立刻切分。

### 关键模式
- **最后出现位置预处理**：先用字典把字符约束变成下标边界。
- **动态右边界**：扫描过程中不断扩展当前片段必须覆盖到的位置。
- **边界闭合即切分**：一旦当前位置等于右边界，就得到一个尽可能短、也让片段数尽可能多的合法片段。

### 复杂度
Time: O(n)
Space: O(1) 或 O(k)，其中 k 是字符种类数；本题小写字母下可视为 O(1)。

### 下次注意
- 完成后删除 `# TODO: implement here`。
- 函数和 `run_tests` 之间保留两个空行即可，当前有三个空行。
- 可以补充一个变量注释说明 `end` 是“当前片段内所有字符最后位置的最大值”，这比逐句翻译循环更有价值。

## Problem 055: Non-overlapping Intervals

### 题型
Array / Sorting / Greedy / Intervals

### 我的表现
- 正确识别出本题应该按区间结束位置排序，而不是按开始位置排序。
- 正确维护 `prev_end` 表示当前已保留区间的结束位置。
- 正确使用 `start >= prev_end` 判断边界相接但不重叠的情况。
- 可见测试和随机暴力对拍测试全部通过。

### 正确思路
- 将区间按结束位置从小到大排序。
- 从左到右扫描区间，优先保留结束更早的区间。
- 如果当前区间起点大于等于上一个已保留区间的结束位置，则当前区间可以保留，并更新 `prev_end`。
- 否则当前区间与已保留区间重叠，需要移除，计数加一。

### 关键模式
- **区间调度贪心**：为了保留尽可能多的不重叠区间，优先选择结束位置最早的区间。
- **边界相接不重叠**：本题中 `[1, 2]` 和 `[2, 3]` 可以同时保留，所以判断条件是 `start >= prev_end`。

### 复杂度
Time: O(n log n)
Space: O(1) 到 O(n)，取决于排序实现和是否允许原地修改输入。

### 下次注意
- `intervals.sort(...)` 会原地修改传入列表，如果题目或调用方要求不改变输入，可以改用 `sorted(intervals, key=...)`。
- 函数和 `run_tests` 之间保留两个空行即可，当前有三个空行。

## Problem 056: Minimum Number of Arrows to Burst Balloons

### 题型
Array / Sorting / Greedy / Intervals

### 我的表现
- 正确沿用了按区间结束位置排序的贪心策略。
- 正确把当前箭的位置维护为当前未覆盖气球的右边界。
- 正确使用 `start > current_arrow_position` 判断是否必须新增箭。
- 正确处理边界相接的情况：`start == current_arrow_position` 时仍可被当前箭射爆。
- 可见测试和随机暴力对拍测试全部通过。

### 正确思路
- 将气球区间按结束位置从小到大排序。
- 对第一个未被当前箭覆盖的气球，把箭射在它的结束位置。
- 如果后续气球的起点小于等于当前箭位置，说明它能被当前箭射爆。
- 如果后续气球的起点大于当前箭位置，说明必须新增一支箭，并把新箭放在该气球的结束位置。

### 关键模式
- **区间结束位置贪心**：把选择点放在当前区间右边界，使它尽可能覆盖后续区间。
- **覆盖判断**：气球能被当前箭覆盖的条件是 `start <= arrow_pos`。
- **新增资源条件**：只有当 `start > arrow_pos` 时才需要新增箭。

### 复杂度
Time: O(n log n)
Space: O(1) 到 O(n)，取决于排序实现和是否允许原地修改输入。

### 下次注意
- 完成后删除 `# TODO: implement here`。
- `points.sort(...)` 会原地修改传入列表，如果调用方不希望输入被改变，可以使用 `sorted(points, key=...)`。
- 注释可以进一步压缩，只保留不变量：`current_arrow_position` 表示当前箭能覆盖到的一组气球的射击位置。

## Problem 057: Merge Intervals

### 题型
Array / Sorting / Intervals

### 我的表现
- 正确使用按区间起点排序的思路。
- 正确维护 `merged` 结果列表，并用 `last` 表示当前正在合并的最后一个区间。
- 正确使用 `last[1] < interval[0]` 判断不重叠，因此边界相接会被合并。
- 正确在重叠时更新 `last[1] = max(last[1], interval[1])`。
- 可见测试和随机性质测试全部通过。

### 正确思路
- 先按区间起点从小到大排序。
- 从左到右遍历区间，结果列表的最后一个区间就是当前可能继续扩展的区间。
- 如果当前区间起点大于最后区间终点，说明不重叠，直接加入结果。
- 否则说明重叠或边界相接，需要扩展最后区间的右边界。

### 关键模式
- **起点排序 + 维护结果尾部**：新区间只需要和 `merged[-1]` 比较。
- **合并条件**：`current_start <= last_end` 时合并。
- **右边界扩展**：合并时取两个结束位置的最大值。

### 复杂度
Time: O(n log n)
Space: O(n)，用于存放合并后的结果。

### 下次注意
- `last = interval` 可行，但它依赖列表引用；更常见的写法是直接操作 `merged[-1]`。
- `intervals.sort(...)` 会原地修改输入；如果需要避免副作用，可以使用 `sorted(intervals, key=...)`。
- 行内注释前建议保留两个空格，例如 `interval  # comment`。

## Problem 058: Insert Interval

### 题型
Array / Intervals

### 我的表现
- 正确把区间分成左侧、右侧、重叠三类。
- 正确使用 `inserted` 标记，避免 `new_interval` 被重复加入结果。
- 正确处理空列表、新区间被完全覆盖、新区间在末尾、新区间在开头等情况。
- 一开始混淆了“不重叠”和“边界相接”的区别，后来修正了测试期望。
- 可见测试全部通过。

### 正确思路
- 从左到右扫描已排序且无重叠的区间。
- 完全在新区间左边的区间可以直接加入结果。
- 和新区间重叠的区间只用于扩展 `new_interval` 的左右边界。
- 第一次遇到完全在新区间右边的区间时，先插入合并后的 `new_interval`，再加入当前区间。
- 如果扫描结束后还没有插入 `new_interval`，说明它应该放在结果末尾。

### 关键模式
- **三段式区间分类**：左侧区间、重叠区间、右侧区间。
- **插入状态标记**：用 `inserted` 表示合并后的新区间是否已经进入结果。
- **边界判断**：本题边界相接要合并，所以左侧条件是 `end < new_start`，右侧条件是 `start > new_end`。

### 复杂度
Time: O(n)
Space: O(n)，用于存放结果列表。

### 下次注意
- 不要只为 `intervals == []` 写特判；更通用的收尾逻辑是“循环结束后如果还没插入，就补上”。
- 当题目说边界相接也合并时，`[1, 5]` 和 `[6, 8]` 不应合并，`[1, 5]` 和 `[5, 8]` 才合并。
- `return result` 和下一个顶层函数之间保留两个空行。

## Problem 059: Meeting Rooms II

### 题型
Array / Sorting / Heap / Intervals

### 我的表现
- 正确先按会议开始时间排序。
- 正确使用最小堆维护会议室结束时间。
- 正确使用 `rooms[0] <= start` 判断会议室能否复用。
- 正确处理空列表、边界相接复用、全部重叠、乱序输入等测试。
- 可见测试全部通过。

### 正确思路
- 按会议开始时间从小到大处理。
- 最小堆中保存会议室当前占用到的结束时间。
- 如果最早结束时间小于等于当前会议开始时间，说明可以复用一个会议室，先弹出旧结束时间。
- 无论复用还是新开会议室，都要把当前会议结束时间放入堆。
- 堆长度最终表示需要创建的会议室数量。

### 关键模式
- **动态最小值**：需要频繁知道“最早结束的会议室”，用最小堆。
- **复用条件**：`earliest_end <= current_start`，等于时可以复用。
- **排序 + 堆**：排序负责按时间推进，堆负责管理当前可复用资源。

### 复杂度
Time: O(n log n)
Space: O(n)

### 下次注意
- 当前写法每次最多 `heappop` 一个结束时间，答案是正确的，但堆里可能保留已经结束的会议室结束时间；可以把堆理解为“已创建会议室的结束时间状态”。
- 如果想严格让堆只表示“当前正在进行的会议”，可以在处理每个会议前用 `while rooms and rooms[0] <= start` 弹出所有已结束会议，并额外维护 `max_rooms`。
- `intervals.sort(...)` 会修改输入列表；如果不希望改变调用方传入的数据，可以使用 `sorted(intervals, key=...)`。

## Problem 060: Kth Largest Element in an Array

### 题型
Heap / Array

### 我的表现
- 正确使用最小堆维护当前见过的前 k 大元素。
- 正确限制堆大小为 k。
- 正确使用 `heapq.heapreplace` 替换堆顶，而不是先 push 再额外 pop。
- 正确处理重复元素、负数、`k == 1`、`k == len(nums)` 等情况。
- 可见测试和额外边界测试全部通过。

### 正确思路
- 遍历 `nums`，用一个最小堆保存当前前 k 大元素。
- 当堆大小小于 k 时，直接把当前元素加入堆。
- 当堆大小等于 k 时，只有当前元素大于堆顶，才说明它有资格进入前 k 大。
- 替换堆顶后，堆仍然保存当前前 k 大元素。
- 遍历结束后，堆顶就是第 k 大元素。

### 关键模式
- **Top K 最小堆**：找第 k 大时，用大小为 k 的最小堆。
- **堆顶语义**：堆顶不是当前最大值，而是当前前 k 大里的最小值。
- **替换条件**：只有 `num > min_heap[0]` 时才需要替换。

### 复杂度
Time: O(n log k)
Space: O(k)

### 下次注意
- `import heapq` 建议放在文件顶部，和 `from typing import List` 放在一起。
- 变量 `l` 可以直接用 `len(min_heap)` 替代，减少一个需要同步维护的状态。
- 当前代码依赖题目保证 `1 <= k <= len(nums)`；如果做工程函数，可以考虑显式检查非法输入。

## Problem 061: Top K Frequent Elements

### 题型
Hash Map / Heap / Array

### 我的表现
- 正确使用 dict 统计每个元素出现频率，没有使用 `collections.Counter`。
- 初版使用排序列表模拟堆，结果正确但没有完成 Heap 模式训练。
- 修改后正确使用 `heapq.heappush` 和 `heapq.heapreplace` 维护大小为 k 的最小堆。
- 正确返回堆中保存的元素本身，而不是频率。
- 可见测试和额外边界测试全部通过。

### 正确思路
- 第一遍遍历 `nums`，用 dict 得到 `num -> frequency`。
- 遍历频率表，用最小堆保存当前频率最高的 k 个元素。
- 堆中元素使用 `(frequency, num)`，让堆顶表示当前 top k 里频率最低的元素。
- 如果堆未满，直接加入当前元素。
- 如果堆已满，只有当前频率大于堆顶频率时，才替换堆顶。
- 遍历结束后，堆中所有 `num` 就是答案，顺序不限。

### 关键模式
- **频率表 + Top K 堆**：先把比较依据从原数组转换成频率表。
- **大小为 k 的最小堆**：找频率最高的 k 个元素时，淘汰当前 top k 中频率最低的元素。
- **堆顶语义**：`min_heap[0][0]` 是当前保留元素中最小的频率。

### 复杂度
Time: O(n + m log k)，其中 m 是不同元素数量。
Space: O(m + k)

### 下次注意
- `heapq` import 放在文件顶部是对的，但顶层 import 和函数定义之间保留两个空行。
- 顶层函数之间也保留两个空行。
- 注释中“堆里剩下的元素”可以更精确地写成“堆里保存的 top k 元素”。
